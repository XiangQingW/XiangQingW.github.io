---

title: 如果编程语言也以编程语言编写，则第一个编程语言如何写入？
date: 2022-01-23T22:08:11+08:00

---




## 如果编程语言也以编程语言编写，则第一个编程语言如何写入？  
### 回答 1
这是一个有点有趣和真正的轶事。  
在70年代末，我正在与研究实验室一起工作，并且给出的任务之一是从组件中建立微处理器板。各个芯片，电阻器，电容器和PC板。 （处理器是英特尔8085  - 当时全新）。这几十年后仍然让我感到惊讶，因为一个未经训练的学生，我设法努力工作 - 好吧，它确实如此。  
所以，我有一个功能微处理器板。最初，作为输入设备，它具有一个小十六进制键盘（零九，a到f，输入键，我认为它有一个退格 - 不确定），并且作为输出设备它具有多个字符的LED分段显示能够单行文本。 （随后输入和输出设备都被更好的选项替换，但这是第一个配置）。  
我设法在Web上找到以下图像。那个我建造的那个肯定是类似的。  
唯一的接口“语言”是机器代码。不是装配 - 机器代码。机器语言的字节。虽然很短暂，但它在十六进制中编写了微小的节目，并让他们展示东西，但快速脱落。所以，当然，这是70年代，而不是60年代早期，下一个目标是使它理解一个编程语言 - 所选的一个被解释的基本。我们发现了一篇日本人（我认为）发布的文件，该文件有一个完整的基本解释器，用于机器代码中列出的处理器。我仍然记得有趣的是，纸质标题下面是一条舌头脸颊线，读过Copyleft，保留所有错误。 （编辑 - 有人找到了原始文章。李晨王于1976年发表了他的小型基础，他是中国人，而不是日本人。李晨王维基百科）  
我键入整个程序，并将其刻录为支持系统支持的EPROM（可能是8或16千字节）。我一定是正确的，因为从那时起就可以与微处理器以基本交谈。  
因此，对您的问题的一个可能的答案：通过在机器语言中实现它，可以将其实施，手动编码每个字节，并将其刻录到EPROM（或其他可编程存储器设备）中来实现第一个编程语言。  
### 回答 2
所有代码都已成为“机器语言”。这是一系列在处理器的本机代码中写入的一系列指令，以及您正在运行的硬件系统。  
回到当天，之前的“语言”，你在汇编程序中编程，这使得在机器语言中编码更加理解，方便，但仍然最终写作了机器指示，这是非常繁琐和劳动密集的。  
这是它看起来的样子：  
这些助记符如“LR”（加载寄存器）和“LA”（负载地址）直接相当于机器语言。  
第一种语言（例如Fortran和Cobol）是Progr  
所有代码都已成为“机器语言”。这是一系列在处理器的本机代码中写入的一系列指令，以及您正在运行的硬件系统。  
回到当天，之前的“语言”，你在汇编程序中编程，这使得在机器语言中编码更加理解，方便，但仍然最终写作了机器指示，这是非常繁琐和劳动密集的。  
这是它看起来的样子：  
这些助记符如“LR”（加载寄存器）和“LA”（负载地址）直接相当于机器语言。  
第一种语言（例如Fortran和Cobol）是用汇编程序编写的程序，它将人类可读指令作为输入并将其编译成可执行的机器语言。沿外的更高级别语言如PL / 1采用相同的方法。这些降低了培训要求，急剧缩短了开发和测试周期。  
编译器只是一个像其他一切一样的程序，区别在于它专门用于创建其他可执行程序。  
### 回答 3
这是一个有效的问题。我可以看出它似乎是如何存在某种循环依赖或鸡肉或鸡蛋的情况。NOPE.NOPE。它完全是这样做的吗？我会尝试解释，但它会花点时间，图表和图片如此，请忍受我一个位。在部分中断行之后，下面的DR下降开始（1940年代和1950年代）非常简单。最简单的工程师实际上的工程师在面板上翻转开关或将电缆插入开关电路。在他们使用的打击卡上，卡片中的孔可以代表打开或关闭的二进制状态（1或者0）。最初没有小鼠，键盘，甚至屏幕（除了非常早期的实施中的示波器类型屏幕之外）。可以在手动读出或卡片设备上打印或显示出来的。用想法将这些设备用于毛皮构建和设计其他输入设备，键盘，小鼠，诸如监视器，打印机等的输出设备是在它之前的技术年份和建立它之前的逐步构建，因此可以拥有什么我们今天考虑一个现代化的计算机。全面看一台非常早期的电脑，即1945年大约1945年被称为巨型标记2的代码：  
非常手动。使用电缆和旋钮，开关以及可能读出数据的纸带，甚至只是打印出与最终计算（不像算盘不同的旋钮打印出来的纸带以下是IBM 533打卡系统的IBM 650大型机（打卡卡甲板位于右侧 -  650次操作员控制台）。这是大约1956年：  
不要太好。非常手动，没有真正的屏幕或任何类似于我们今天会叫电脑的东西。无论何地，它都有许多进步，加载打卡程序的能力，更快地运行，更好的指令集等......也是如此添加IBM 653存储单元的能力，该存储单元添加了磁芯存储器，60个10位字的磁芯存储器，3个数字索引寄存器和浮点算术精度。指令集更进一步，并且具有汇编语言的基本助记符功能稍后会有。如果我们向大约1968年移动到大约1968年，我们会看到类似HP 9100的东西：  
嗯。这开始看起来像一个电脑。of.techly，它被称为第一个科学计算器，用于营销原因，但它基本上是第一个桌面电脑之一。它有一个纸磁带读数，小屏幕和键盘输入数据。这是Time.Circa 1971的众多第一个可编程计算器或个人桌面计算机之一。这里我们拥有IBM 360大型机和控制台：  
这现在开始看起来更像是我们将考虑的电脑。这是一个典型的输出监视器，以及输入的典型键盘。鼠标尚未发明。1981年我们拥有IBM的第一个运行英特尔的官方个人台式电脑处理器：  
TL;博士  
我的观点是每个一代都在上一个上面建立.Langumages作为编程计算机做有意义的东西的方式。目前来自翻转交换机的艰苦任务，以获得所需的效果（编程）。那是说，我们今天做的键入我们今天做的软件，或者这瞬间做了什么，只是一个非常先进和抽象的版本的同一件事。我们认为这主要是由于：1）今天的输入设备非常顺畅，光线，快速，响应和2）通过我们选择的语言的给定指令我们可以更加明显地做，检索更多的数据，产生有时的产出量（相对于2-5年前），这也是3的结果）今天的计算机比甚至70年代的最佳电脑速度快。秒）.Thats 3 -6百万次作为1975年最佳电脑的速度快！它的数量级100倍，与1945-50的第一台电脑相比。吹嘘，嗯？  
### 回答 4
考虑这一点的一种方法是你想制作一些非常强大的砖头机智  
另一个不完整的提供......  
它值得深入启动，因为它有助于获得元杠杆思想的经验。例如，如果只有一台计算机只有一台机器代码？在这里，您必须使用与机器代码指令对应的位模式。如果你懒惰和雄心勃勃的同时怎么办？ IE。你不想在比特中捣碎比你必须多，你还希望尽快制作一个高级编程语言？  
一种方式来思考这一点是你想制作一些非常强大的砖块，没有大量的手工编码，那么允许引导引导所需语言的东西（并且也许你需要一个引导阶段） 。  
从60年代我们可以随意使用多台计算机，因此我们可以考虑使用更多工具的途径。人们写交叉编译器，但我倡导这是最后一件事。在游戏的早期部分引导各种口译员，您更好的是。  
一个有趣的皱纹是在我们的开发机器上写一个目标机器的翻译。这使得很多意义，因为在我们的开发工具的怀抱中可以在目标上删除目标之前可以在我们的武器中完成。  
我在1965年在国家大气研究中心编程时，我有乐趣。头系统程序员没有信任控制数据，因此他们决定为不完全发布的6600个超级计算机进行自己的系统软件，并让我在我们现有的CDC 3600上写一个模拟器 - 所以它可能是调试。与大多数HW架构相比，6600是非常激进的，并且很有趣，使得能够迅速模仿机器（这是用3600个汇编代码编写以获得最大速度）。  
这结果是出现的，因为这是在这之后不久的是危机公告，尚未为6600个超级计算机提供疾病委员会。那些将获得这些机器的人 -  Livermore，国家大气研究中心，桑迪亚，洛杉矶阿拉莫斯等。 - 汇集他们在疾病疾病委员会外面所做的事情，看看他们可以想出什么。我们都在搬到Chippewa Falls Wisconsin（Seymour Cray的家乡，以及他的实验室）一年的良好部分搬到Chippewa瀑布（Seymour Cray的家乡）将允许使用机器的软件套件。  
我写的6600个模拟器再次被压入服务 - 我在6600个汇编代码中重写它（使用已在我的3600仿真器上调试的NCAR汇编程序），以便为6600进行调试器（Cray忽略了中断机器，它很难控制）。该模拟器尽可能多地使用机器本身以保留速度（与主内存循环的主内存如何交织在一起 -  6600可以运行主存储器速度的10倍）。  
底线在这里是，在引导时模拟目标HW似乎是很多工作，但它允许更好的开发和调试，因此最终引导的是一个自给自足的系统。  
同样，即使在要编译时，它始终有助于模拟语言。这在SmallTalk的经验中得到了帮助，它的举射冒险经验（例如，如何进行吱吱声和部署）。  
### 回答 5
从那里，有B.  
机器语言是一种编程语言，但与其他编程语言不同，它本身并不是用另一种语言编写的。相反，在计算机的电路中嵌入了对机器语言的支持。在其他事情中，设计计算机涉及挑选机器语言，并确保计算机被连接到执行该机器语言的程序。  
因此，第一个程序在机器代码中，使用一束按钮和开关在计算机内存中输入，或者在交换机上以某种方式接线引脚。  
从那里，有一个引导过程，人们编写了将更轻松地编写新程序的程序。一个重要的第一步是汇编程序。汇编程序组装来自扬声器指令的机器代码，对人类进行了很多更容易读取的，将新程序的开发时间缩小为大量因素。  
在汇编者的帮助下，开发了更复杂的语言，从底层机器代码中逐渐更加摘要，越来越多地突破。  
请注意，此精心启动过程必须仅发生一次。如果现在发明了一台新计算机，我们只需用我们喜欢的任何语言编写程序，即使用已经存在的软件的其他计算机来编写程序。我们所要做的就是确保旧计算机上现有的编程语言可以将程序转换为新的计算机的机器语言。  
### 回答 6
让我们有一个简单的例子，四语言。人们可以在机器代码中写出第四个地址解释器，除了一个人的想象和理解之外，没有任何东西。当您有此时，您可以在机器代码中写入，但是在第一个代码中，并且相当快地创建外部解释器 - 可以读取和解释最重要的源代码的非常受限的子集。有一次，您可以输入文件并将您的语言增长到您喜欢的内容。包括一个良好的汇编语言（FO ...中的一个寻呼机......  
### 回答 7
一个子集是用汇编语言编写的。然后使用该子集启动整个语言的编译器。随着新功能的可用，编译器升级以使用它们。  
在更现代的时代，可以使用一种语言来编译另一种语言，避免需要连续更新。  
### 回答 8
这里已经很多答案，但我仍然会加入我的战争故事。  
我的第一台电脑是传奇的Commodore 64：  
......当时，几乎所有家庭计算机都会有一个基本的解释器内置于其ROM中，C64也不例外。你可以用基本的写作小型简单的程序来尝试写一些游戏，但你很快就跑进了一个昭着的赛事：基本的程序是方式，太慢了，做任何有趣的动作游戏。在80年代，几乎任何电脑游戏都会用组装写，基本上是CPU机器代码的助记符词汇。通过查找每个指令类型和参数，可以通过查找操作码来机械地将汇编语言进行翻译。通常，这是通过称为汇编程序的程序来完成，该程序在汇编语言文本文件中读取并占用机器代码的字节。  
我当时没有一个花哨的汇编者，但我想写得很糟糕，所以用铅笔在纸上用汇编语言写下我的节目。当我非常确定它是正确的时，我会手动为每行装配编写机器代码，然后将其戳进入机器并运行它。如果它没有工作，它就回到了纸张并手动证明我的涂鸦，再次转换为机器代码等。有时候最终经过很多努力工作，胜利！  
毋庸置疑，这是非常乏味和效率低下的。  
这是在您可以从互联网下载的东西之前，因此当时计算机杂志有页面源代码，您可以在几个小时的键入后输入，您将在您的计算机上进行程序（除非您制作单个打字错误它不会运行）。有时它将是基本的有时，它将依从性的机器代码编号页面，你必须以100％的精度戳戳。  
有一天，LO和Beold，一本杂志发布了一个汇编者，经过大量打字我现在能够在实际机器上以汇编语言写入我的程序而不是笔和纸。进步！  
您可以非常流利地流利地流利的语言，尤其是CISC，因此一旦您有一个汇编程序，实现像C这样的简单语言并非那么具有挑战性。  
所以你有它。您可以通过用铅笔在纸上撰写装配器来引导Von Neumann架构芯片的PCB;将机器码戳为RAM。它不是比在杂志中的几页上写的更复杂。接下来，使用汇编程序实现更高级语言编译器并开始编写操作系统。  
### 回答 9
第一个编写的编程语言没有写入，它们是构建的。数字计算机实际上是一种特定编程语言的电子实现，称为该计算机的机器语言。  
要更容易拼写这一点，数字计算机是处理数字的设备，其存储在称为指令寄存器的地方，作为指令。每个数字都会导致计算机执行不同的动作。该操作由电线和电子组件实现。以下是一个非常原始计算机中可能的机器语言动作的一些示例。  
000：什么都不做;加载下一个指令并继续。  
001：将1添加到寄存器1中编码的数字。  
002：从寄存器1中编码的数字中减去1。  
003：如果寄存器1中编码的数字为0，则停止。  
004：将寄存器1中编码的数字移动到指令寄存器中，并将其视为下一个指令。  
等等。  
该概念的重要方面是数字和动作之间的映射是完全任意的。  
每种类型的计算机都有自己的机器语言，最早的程序是用这些机器语言编写的。他们非常难以阅读：典型的程序看起来像：  
007 496 231 000 002 431 007 001 509 306 772 ...  
继续数百或数千个条目。在人们意识到的情况下，可以编写将更可读描述的程序进行翻译成机器语言的程序，并且开始开发更高级别语言的程序。  
要返回基本点（也是在其他答案中的一些答案中进行的），数字计算机实际上是一种实现编程语言的机器。一旦你建造了一台电脑，你就不需要编写一个编程语言来开始，你有一个坐在你面前。  
（脚注：在现代计算机中，即使是基本机器语言指令实际上，如果您在电路内部深度看，则由用微码中写入的程序和由处理器芯片内置的小型小计算机执行的程序来实现。）  
### 回答 10
使用交换机或引脚在二进制中编程第一台计算机（数字二进制电子计算机，迂腐）。我已经像那样编写了ferranti fm1600b。 CPU非常简单，所以指令集很简单。例如。 001的操作码意味着添加和010意味着减去。 CPU使用寄存器，它们以零开始编号，因此您可能有代码  
<OL> 001 001 010  - 添加寄存器1到寄存器2 010 001 000  - 从寄存器零011 010 101  - 存储寄存器2  - 在RAM地址5 </ OL>中的存储寄存器2  
<Li> 001 001 010  - 添加寄存器1注册2 </ li>  
<Li> 010 001 000  - 从寄存器零中的减去寄存器1 </ li>  
<Li> 011 010 101  - 存储RAMAT ADDREND 5 </ LI>中的存储寄存器2  
我已经简化了 - 即使是非常简单的计算机也有超过8个RAM位置  
作为德乐  
使用交换机或引脚在二进制中编程第一台计算机（数字二进制电子计算机，迂腐）。我已经像那样编写了ferranti fm1600b。 CPU非常简单，所以指令集很简单。例如。 001的操作码意味着添加和010意味着减去。 CPU使用寄存器，它们以零开始编号，因此您可能有代码  
<OL> 001 001 010  - 添加寄存器1到寄存器2 010 001 000  - 从寄存器零011 010 101  - 存储寄存器2  - 在RAM地址5 </ OL>中的存储寄存器2  
<Li> 001 001 010  - 添加寄存器1注册2 </ li>  
<Li> 010 001 000  - 从寄存器零中的减去寄存器1 </ li>  
<Li> 011 010 101  - 存储RAMAT ADDREND 5 </ LI>中的存储寄存器2  
我已经简化了 - 即使是非常简单的计算机也有超过8个RAM位置  
当WordLengths变长（8位，16位，24个）我们将二进制数字组分组为八进制（稍后的十六进制），以使它们更容易读取，并将操作码写为文本，而是用手转换为计算机进入计算机  
<OL> 112 = 001 001 010 210 = 001 001 010 325 = 001 001 010 </ OL>  
<Li> 112 = 001 001 010 </ Li>  
<li> 210 = 001 001 010 </ Li>  
<Li> 325 = 001 001 010 </ Li>  
我的第一台PC是SC / MP开发套件。它有一个十六进制键盘，所以我用手汇编程序在汇编程序中，将它们翻译成Hex，然后将它们键入。后来，我有一个基本的解释器。我实际上没有写过那个，但我写了界面代码和加载程序，以便我可以将解释器刻录到EPROM，以及读取和写入盒式录像机的代码，以及手动在屏幕上显示文本的代码在汇编程序和十六进制。  
下一步是开发电网和读卡器等I / O设备，让计算机读取字母表（通向ASCII）。然后，您可以使用计算机本身将Mnemonic Opcodes转换为二进制汇编语言。  
<OL>添加R1 R2  -  001 001 010 SUS R1 R0  -  010 001 000 STO R2 5  -  010 001 000 101 </ OL>  
<li>添加R1 R2  -  001 001 010 </ Li>  
<Li> Sub R1 R0  -  010 001 000 </ LI>  
<li> sto r2 5  -  010101010 101 </ Li>  
汇编语言具有1：1与STO（商店）的1：1对应于数字（3或011）。有些名称是固定的（例如，STO），而其他名称则分配其他名称，例如，特定内存地址的标签可以用作变量的名称（您仍然必须知道它是一个字节或单词等）或作为分支操作的目的地。然后，汇编器可以处理计算分支偏移量 - 而不是写入BR 12跳转到内存中更高的指令12八进制字节，您可以将BR结束跳转到名为END的标签。当然，第一个汇编程序是用手工编写的机器代码。  
编写相同的代码块遍及执行简单的事项，如将整数转换为ASCII是耗时的，因此创建了一个宏汇编程序，可以存储代码或数据块并通过一个名称引用整个事物。  
当开发出与机器代码没有1：1对应的福特兰语或COBOL等编程语言时，编译器是用汇编程序编写的。稍后，C编译器用C写入C，然后通过手工转换为汇编程序来引导到计算机中。一旦运行，它就可以编译自己。 （实际上，C的历史略微更复杂 - 每个维基百科）  
这导致Ken Thompson Hack的可能性 - 如果编译器以自己的高级语言编写，并且编译器中存在一个后门，它每次编译它自己都可以添加额外的代码，它可以添加额外的代码，它会切勿出现在源代码中。发现它是拆卸它的唯一方法是用手从二进制（包括所有链接库）拆卸。  
如今，一台机器存在编译器，为了将其移植到新的体系结构，它不需要在汇编程序中从头开始重写，所有它需要的是使用相同的语法解析器等新的架构特定的输出模块。实例，我已经在英特尔X86架构上使用了一个C编译器来编译ARM芯片的代码。  
### 回答 11
有困难。  
第一个程序是用汇编程序编写的，然后用手转换为二进制。然后，二进制将使用交换机和按钮使用手动输入计算机，甚至通过将插头插入插件中。  
这启动了一种称为自动启动的序列，其中这种方法用于写一个简单的汇编程序。反过来，这是用来编写一个更好的汇编程序，它用于编写早期高级语言，稍后写道。  
但开始是艰苦的手工工作。  
### 回答 12
如果编程语言也以编程语言编写，则第一个编写的语言是如何写入的？  
第一个编程语言实际上没有写入任何内容。它涉及手动重新加速计算机的组件。显然，这是笨重的。为了解决这个问题，工程师创建了一个解释馈送到它的指令的处理器。这是机器语言在打击卡编程时代。  
快进一点，并且汇编器是发明的。之后，可以在相对容易的装配而不是机器语言中创建编译器和解释器。现在，大多数编译器和解释器都以更高级别的语言创建（C是一个流行的选择），并且可以将编译器引导以编写编译自己的编译器。  
### 回答 13
在学校在学校的电气工程系中学习时，有数字系统课程，教科书是Mano的逻辑和计算机设计基础。在本课程中，涵盖的最后一个主题正在设计一个单循环计算机。步骤，单个循环计算机的设计过程对机器语言及以后的最低级别编程语言提供了一个好主意，因为汇编语言也提供了更高的级别编程语言和基本等语言也是如此。  
从逻辑和计算机设计基础书/ Mano所拍摄的数字。  
说明（添加，减去，乘法，写入内存，从内存中传输数据）被定义并存储在存储器中的内存中。使用这些指令集（编写某些组合和零和零有意义，在哪里写下这些，零有不同的含义！）并使用机器语言，寄存器（存储一组位或字节信息）操作，alu（算术逻辑单元）操作，内存地址，要分配的变量，实现了任何类型的内存操作。进一步是形成汇编语言以用符号和存储器地址替换二进制操作，以及符号的总线地址。  
在汇编语言中，定义了所有硬件操作。交易和编程微控制器需要大多数时间使用汇编语言进行操作。直接注册和内存操作是在指令集操作中完成的，符号语言取代二进制操作码和具有符号名称的地址。  
与机器和汇编语言相比，编程语言的差异（比C语言更高），没有必要知道硬件，记忆一般的计算机设计！有函数，Ready库和库函数来编程无论您想要的内容。这些函数，库由在装配中编写的许多代码组成，它们显然将与机器语言转换为无误解的硬件通信。函数，库都是由集合语言操作组成的。这些编程语言中的函数的名称在语言的创建过程中确定。  
作为最后一句话，高级编程语言提出了复杂的GUI（通用用户界面）的IDE编译，运行和测试编写的程序。在高级编程语言IDE中，有一个编译器将编程代码，函数，库转换为机器语言，使硬件理解并做任何您想要的！  
### 回答 14
在最基本的电平电脑硬件上只不过是电子电路。称为CPU的集成电路芯片具有数百万个门，现代CPU非常复杂。  
然而，您的问题是关于兰加格和源语言之间的基本面和明显的循环依赖关系。 C和C ++仍然是涉及编译器和内核源代码等内容时的主导系统语言。  
现代C ++编译器使用以前版本的编译器编译。因此，唯一真正的困难是了解第一个C编译器是如何写的。它肯定用机器代码编写，并针对主机平台进行了优化。  
所以问题成为你说的这款机器代码是什么？机器代码只不过是编码指令和数据的二进制模式。  
CPU在某些架构上有用于在CPU寄存器和内存之间加载和存储数据的说明，还有用于对设备执行输入输出的特殊说明。  
有一块名为汇编程序的软件。在GNU系统中，通常称为气体。它需要Mnemonics MOV（移动）或添加。确切的指令集由CPU设计器确定。  
因为不同的架构是自由实施不同的指令集，而且我们不想抑制CPU设计的创造力，是需要更高级别语言的根本原因。  
如果我们能够强加一个真正的计算机架构OTCA，我们可能仍然可以使用汇编程序完成。但幸运的是，我们已经了解抽象的早期优势，而是隐藏了机器代码的杂乱细节，因此我们可以专注于我们实际尝试解决的更高级别问题。  
如果你想真正了解我暗示的问题的答案。我会鼓励您阅读一本关于基本CPU设计的书。在8086，80386家庭CPU家庭上写了许多好书。它们都非常陈述，但数量级比最新的英特尔CPU的数量幅度不那么复杂。  
### 回答 15
您会将源代码写为（纸张）编码表上的汇编级指令，将这些指令键入打孔卡上，然后调用计算机的汇编器（这本身是一张大甲板）。汇编程序将读取您的源代码并创建称为Object Deck的另一组打印卡，其包括要执行的实际硬件指令。然后使用计算机的读卡器将对象甲板加载到内存中。不需要操作系统。但是，它也取决于撰写代码以允许程序访问任何I / O设备（例如，磁带驱动器，打印机等）。第一计算机I编程为IBM 1440，只有4000字节的RAM（并且内存实际上由微小的磁芯制成 - 因此我们仍然使用术语核心存储器的原因）。但它耗尽了一家小型制造公司的所有业务应用。我甚至为该系统编写了一个程序，该系统使用了预测产品需求的最小二乘回归分析（和男孩是慢的）。所以我想问题的答案是，有人用这个相同的进程写了一个第一个编程语言，它创建了一个可执行的编译器，它反过来会理解您创建的编程语言。  
### 回答 16
好的，所以我们从二进制开始.. 0和1。这不是一种语言;更像是处理器中一束交换机的开/关状态的表示。其中1亮，0关闭。在二进制文件中工作需要程序员真正了解他们正在处理的计算机的CPU架构和机器的硬件。二进制文件的第一个问题是写一个小程序来做一个现代任务是非常长的，这需要大量的代码。  
因此，我们从十六进制表单中开始不同地代表二进制。这使得代码基本更小，但没有地解决最大问题：二进制尚未易于阅读，理解或维护。  
要使我们开始使用汇编语言，让事情变得更轻松。它仍然非常接近机器电路，需要深入了解您使用的硬件。组装的第一个编译器可能是使用二进制/十六进制进行的。  
所以现在我们有一种语言可以让硬件与硬件相互作用，但它仍然不会允许开发人员轻松解决高级业务任务。为此，我们需要更多的抽象。  
因此，随着语言的进展，它们远离物理硬件，但变得更加可读和可维护。虽然计算也变得更加强大，但它们也变得不那么冗长。  
一个有趣的项目您可能想要阅读的是Roslyn。 C＃语言的编译器最初是用C ++编写的。一旦C＃成熟，它的编译器被从C＃中的划痕重写!!  
所以现在我们有一个编译器，这些编译器是用它的语言编写的！  
所以我知道我在答案中错过了很多细节。 ID欢迎建议编辑或阐述。  
### 回答 17
我们派了我们  
在70年代初，我的高中通过提供计算机编程课程（无论如何）破坏了地面（当地）。  
在第一季度，我们有一些看起来像一个巨大的计算器。它以机器语言编程，一次一点。它有一个小的穿孔读卡器。洞用一个微小的冲床冲压出来。我不记得该模型或任何东西。  
在第二季度，我们学会了Fortran。学区有一个IBM电脑市中心。所有这些程序都在打卡，每行代码一张卡片。我们偷了商业部门的Keypunch机器。  
我们在星期五发送了我们的节目，在一个盒子里，星期一打印出了打印输出。他们在周日上午1点凌晨1点凌晨1点跑了我们的课程，当时电脑时间最便宜。  
当我第一次加入海军时，我们的一些声纳系统使用了迷你电脑。每当我们想要引导系统时，我们都有一系列步骤要手动输入，使用开关和灯光，只是为了让它们从磁盘启动。  
当第一台家庭计算机出来时，Hobby杂志都有列出的程序，以尝试各种任务。一些程序以机器语言为单位，使用十六进制数字，其中一些是汇编语言，使用缩写代码，有些是基本的，它使用了实际的单词和短语，例如goto。  
正如其他人所说，在引导时，运行机械语言解释汇编语言程序。然后加载汇编语言程序，解释基本程序。然后，基本程序可以加载其他更大的程序。这就是我们40年前做的。  
### 回答 18
我相信41个答案，没有理由添加任何东西。即便如此，我也想到了几年前听到的故事关于从机器代码到更高级别语言的步骤之一。故事的标题是自我优化汇编程序汇编程序。  
要了解这个故事，你必须知道一次，有一台工作记忆是鼓内存的计算机。鼓持续旋转。它有很多头。每条轨道由可寻址的滚筒上的特定位置组成。  
程序由代码行组成，后跟下一个指令的地址。每条指令后，这有点像GOTO。在此计算机上置于超级编程器中的技能之一是写入代码的能力，以便在执行指令之后，下一个指令将正确地读取，以便优化执行速度。  
因此，故事来了，一个CAGY程序员开始编写一个汇编的优化器，该优化程序将采用汇编程序，创建机器代码，但也可以移动围绕的说明，以便它们处于滚筒上的最佳位置。但是，他在下一步之后顺序地将该程序写入了每个指令。他的同事们嗤之以鼻，说没有人会使用这个程序，因为它会太慢地执行。考虑到每条指令都需要完整的鼓，浪费时间。  
但程序员忽略了嘲讽，并继续写作并调试他的计划。当他终于结束时，他在猜测上测试了它？他自己的计划。当然需要很长时间才能驾驶。但是，当他完成时，他有一个优化版本的他自己的程序，尽快运行。立即获取名称？自优化汇编程序汇编程序。  
### 回答 19
正如其他人所提到的那样，第一个汇编程序是用机器代码编写的。是的，原始机器代码。第一名编译器是在装配中编写的。后来的编译器是使用早期编译器编写的。  
当开发新机器时，编写了交叉编译器。例如。第一个B6500 Algol编译器写入B5000上运行。使用B5000编译器编译，提供B5000可执行文件。然后使用刚刚编译的B5000代码再次编译（在B5000上）编译，给出B6500可执行文件。然后将此代码发送到B6500，然后b6500然后有一个allol编译器！  
### 回答 20
我一直很长时间要记住，什么时候没有任何编程语言。通过在粘贴上布置电线来完成编程。没有磁盘，没有内存。所有输入和输出都在一个单个单张卡上。  
后来，当我们有一些内存并需要更大的程序时，我们将通过在行二进制中的单个语法卡上编码的原始解释器发送。此解释器读取其余程序，该程序被编码为一系列6位数的八进制数，该数字被翻译成18位机器字。一切都必须在绝对中被编码。如果您想要一个变量，则必须将其分配给特定的内存位置，例如56.然后您将在该变量上操作的每个指令中包含该位置（八进制70）。  
下一步是提供一些符号能力。您可以为说明提供名称，为Add和S for Store表示a。您也可以为您的一些变量提供名称。所以现在你可以说一个x用于将x添加到累加器。汇编程序将决定x的内存位置，这可能会因下一个运行而异。  
这项工作的方式是第一张牌进入机器将是八万翻译。这将读取包含在八进制中编码的汇编器的下一个3到7张卡。然后将读取您的程序，并将其转换为机器代码。  
这就是全部开始的。  
### 回答 21
零和那些，我的朋友。零和那些。  
所写的每种语言最终都会减少到零的零，形成机器语言，母语语言（例如要说的计算机）的处理器。第一个编程语言或多或少只是向计算机发送这些零。然后有人决定写一些像汇编语言一样的东西，这让您使用一小组命令作为一种宏来避免所有零和那些宏。然后有人来了，写了类似的东西，这让你使用更大的命令作为一种宏，以避免装配或机器语言等。每种语言都几乎只是向现代语言几乎像表情符号的地点抽象一堆较低级别的东西：类似的东西:)等于写作我很高兴，我只是开玩笑。  
有时候，当我编程时，我停下了一会儿，考虑我写的是什么可能被转变为别的东西，它正在变成别的东西，直到它最终成为这些零和那些，而且我默默地奇怪它是如何工作的:)  
### 回答 22
HTTP：// Old的Google图像结果  
这是在电子计算的早期几次完成的（即使在存在于集成电路 -  IC和微处理器之前）。  
通常，某种汇编器（迷你，符号或宏）是用组装（在纸上）写入的，并将（手动）转换成机器代码，然后在很大地进入新计算机架构的原型（通常使用桨和开关前面板） 。有时，用于读取纸带或打孔卡或甚至磁带的引导驱动器在装配中编码，手动转换成机器代码，然后输入。  
http://oldcomputers.net/pics/imsai8080-left.jpg的Google图像结果  
这是稍后模型的示例，其围绕英特尔8080构建，但是显示用于输入引导码的开关面板的一种形式。  
这是一个KSR-33 Teletype（TTY），显示键盘左侧的纸带单元。  
Google图像结果为http://www.columbia.edu/cu/computinghistory/teletype.jpg  
TTY可以脱机以录制纸带上的文本，然后将磁带通过读取器慢慢地将磁带慢慢旋转，然后将其作为输入作为输入。  
原始设备驱动程序也以这种方式写入和输入。显然，这些最终被编码为PROMS（通过专门的程序员设备（见下文）。这些通常能够单独使用十六进制键盘，也可以用作现有计算机的外围设备。  
Google Image结果http://farm4.staticflickr.com/3741/9443139558_eae2829501.jpg  
最终建立在此内部的团队来实现相当完整的特色宏汇编程序，然后使用那些构建其他编译器和口译员。 （主要是口译员是后来的奢侈品）。  
一旦他们构建了足够的基础架构，就可以使用该软件（驱动程序，编辑器等，交叉编写旧计算机架构的汇编程序和语言编译器，以了解旧模型和设计。  
您在旧的大型机或迷你上写下代码，将其写入新原型可以读取的媒体，然后在新设备上运行它。对于每个新架构，此过程继续，直到新目标上的工具链足以进行自托管的开发和改进过程。  
遵循相同的过程，用于开发操作系统以及开发工具链。 （这些都是交织的，但硬件和软件侧面有点明显的开发时期......将历史开发的图形图像沿着两驼峰的顶部骑行）。  
它被认为是任何新的计算架构或其操作系统的主要里程碑。这大大提高了该平台的软件开发速率，因为代码可以在同一系统上写入和测试。  
请注意，许多新架构（例如x86-64（amd64）已经在现有，建立的，成熟的自托管平台上使用向后兼容模式开发。  
在这些情况下，该软件最初是在现有（旧的）模式中开发的，并通过运行它并将其转移到更新模式（启用新功能）。对于8086  - > 286和386，这是真的，然后从x86到amd64（486，pentium和其他32位CPU是增量改进而不是新架构）。  
我认为英特尔X86行是使用此策略的最成功系列架构，我认为这是由于主导桌面和服务器架构在很大程度上是由于这一策略提供的（相对）平滑的过渡和市场采用路径而取得了成功。  
### 回答 23
艰难的方式。  
例如，此视频中的家伙使用物理交换机在机器代码中编写一个短程，在他的Altair 8800计算机上的机器代码中：  
我强烈推荐这本书是一个完整的旅游：  
et voila！  
### 回答 24
大约35年前，我得到了一个非常原始的家用电脑，名叫Praae，这是第一个罗马尼亚语 - 然后是共产主义国家 - 个人电脑。它知道基本，有一个Z80微处理器，16 kB（!!!!!）RAM和16 KB ROM。我永远不会忘记，可用的内存是免费的8050字节。不是很多。所以，快速对我写的节目来说是不够的。由于我没有编译器或汇编程序，但知道计算机的内存映射，我开始在机器语言中编写代码，指令指令。我在内存中戳它，并通过访问中断启动代码。突然，大量的记忆变得足够了。  
确实是的。我没有编写编译器或汇编程序，实现编程语言，但这主要是因为我不需要它，而且因为我的知识有限（我约16-17 ......）。但有可能。并且一切都在纸上写成并在印刷Z80指令集的帮助下在纸上转换为Hex代码。用手，byte-byte。  
因此，这就是如何为第一个编程语言编写编译器的方式。  
后来编辑：现在回忆回到了我的脑海里......因为在开始时，我必须为每个字节戳地址，价值<enter>，我写了一个短暂的程序，一旦推出，在连续地址时写了一下我输入的价值。所以，我不得不输入第一个地址，之后是一个接一个地的值。我还使用了一些特殊的键来控制程序，访问新地址范围或运行它。这不是一个编程语言，但它后来被称为开发环境。和我第一次听说很多年后的概念......  
### 回答 25
为每个计算机芯片系列编写了一个汇编语言，例如计算机proc  
1978年，我在Kim-1上以十六进制进行了一些编程。 *十六进制是我们称之为机器语言的。 *否则，您必须使用所有零和那些。  
我使用的下一个语言被称为汇编程序，这是一个助记符语言，并且对机器指令具有强烈的外观，但更像是英文单词的短手版本。十六进制字符1a可能是一个机器代码，用于将值x存储到芯片寄存器a中，并且汇编字是stoa。  
为每个计算机芯片系列编写了一种汇编语言，例如称为ZILOG Z-80的计算机处理器芯片，或罗克韦尔6502，或摩托罗拉6803，以及RCA COSMAC 1804（我们用于开发和专利计算机）受控组合轮椅控制器和Augmenative  - 语音发电机装置，由斯蒂芬抱在20世纪80年代着名。点是，每个芯片都有自己的汇编语言。  
十六进制代码是另一种拼写零和 - 零的方式......在十六进制中计数看起来像00,01,02,03,04,05,06,07,08,09，a，b，c，d，e， F和那些是16个十六进制字符。叫十六进制，因为有16个字符。  
要回答问题...汇编程序是高于十六进制的步骤，我们正在使用命令名称而不是十六进制字符：就像stoa for store x进入a;与STA和STX和Sty .adc一起添加带有位，SBC用于减去随携带位。 JMP用于跳转到存储器位置并读取下一个磁节/数据。 BNE和BEQ用于平等或不等于其他地点的分支。 INC和DEC增加和减少价值。和或eor等。  
### 回答 26
我继续写一个  
我曾经写过一个第四个翻译。我也曾经写过一个元编译器发生器。在这两种情况下，所做的是遵循所生命的代码，如人类脚本，写下生成的输出，然后在硬路（交换机和灯光）中输入二进制文件。一些重复和控制台调试，我有一个工作的最小可行计划。  
然后你长大，使用前一个编译新的。保持细节对有点棘手。这是作为手动过程的递归。因为它是一个字典语言，但继续重新编译。  
我继续使用第四解释器来编写一个原始汇编程序，并与它相同，因为dos等事情尚不存在。 （自己建造了硬件）。  
任何一条手段都没有在全球意义上的第一个。但他们是我的第一个，我想象我们的田地'真正的先驱者是相似的事情。  
顺便说一下：到这一天，我仍然非常钦佩：第四（编程语言） - 维基百科  
优雅。简单。清洁设计。以后沿着同一思维的语言是Smalltalk  - 维基百科和吱吱作响 - 维基百科。  
### 回答 27
通常（和希望），一个编译/口译员无线电  
编译器或解释器是消耗程序（或逐渐逐个一部分）的程序，拒绝程序并产生一些错误解释原因或它们接受程序和输出的一些错误 - 这可以是可执行的或者表示在一些其他语言中的程序，说将python映射到c。  
编程语言是一个数学构造，它描述了可接受程序的语法（即文本是有效的）和一些关联的语义（即，程序意味着什么在某种意义上）。  
通常（并希望），编译器/解释器将以语言接受程序并将其转换为尊重语义的意图的内容。  
所以问题更像是如何获得第一个编译/口译员的方式。嗯，第一个是，在某种意义上是硬件。它接受了电压形式的输入或（可能是通过打孔卡输入，或者从系统中的某个内存中输入某些内存）并将其转换为改变硬件信息状态的操作。在制作第一个软件编译器的意义上，您可以接受机器作为基础（我更喜欢公理地给定的术语）编译器。  
在此之上，您可以在硬件已经了解的语言中，构建一个读取某种形式输入的程序（例如，汇编语言，从而制作汇编器[1]）并输出硬件的指令。您现在有一个以硬件说明编写的程序，可读取输入并输出稍后使用的硬件说明。  
<a> [1] </a>  
现在你有一个汇编程序。使用此，您可以编写C编译器。它甚至不必是一个完整的C编译器，也许它可以实现语言的基础知识并留下一些东西。然后，您可以使用此C编译器来编写完整的C编译器，并使用该编译器，您可以编写一个漂亮的Python编译器/解释器。或者也许您可以编写一个Web服务器和一堆其他东西，并为询问和回答问题进行社交网络。当然，你可以刚刚在汇编程序中写一切，但这可能是乏味的，但它也可以很有趣[2]。  
<a> [2] </a>  
脚注  
### 回答 28
二进制文件读取的计算机只有1s和0s。为了更简单的任务  
第一台计算机很难过。它们基本上由手工编程，通过将电气组件连接在一起，载于电动部件，计算机能够通过打击卡读取给他们的指令。打卡卡包含相当于当今的机器代码 - 代表说明的二进制代码。计算机如何解释每个指令都将每个指令硬于计算机电路。除了代码存储而不是物理卡上的代码而不是在物理卡上存储的情况之外，将计算机实际上工作实际上工作。  
二进制文件读取的计算机只有1s和0s。对于更简单的任务，您可以物理地输入二进制文件以及所需的所有内容。我们曾经在卡片中掌握过电话卡，以便在卡中发出一个洞来表示数字。  
CPU的基本操作是加载指令并执行它;这是硅的设计。每个指令是二进制数，CPU的架构识别这些数字并将它们映射到它应该执行的操作中。  
从理论上讲，您可以只在二进制文件中写一个节目。由于最早的计算机与他们的程序以完全被组件连接的程序硬连线，并且连续几代人然后能够从一行切换开关或冲压卡中读取程序。  
纯机代码的第一个和最自然的扩展是一个汇编语言，其中每个指令给出一个名称而不是数字（更容易记住MUL以乘以超过11110110）。然后是一个非常直接的映射 - 所以要做从汇编程序指令到机器代码的程序非常简单，即使您必须在原始机器代码中写入它。  
接下来是编译的语言，在那里您可以创建待机的关键字，即常用的代码块。因此，而不是必须在逐步写入（例如）进行数学计算，而是将结果放在寄存器中，并检查该寄存器作为条件跳转的基础，您只需写入  
<ol>如果（变量> 0）{做一件事} {做不同的事情} </ ol>  
<li> if（变量> 0）{</ li>  
<li>做一件事</ li>  
<li>} else {</ li>  
<li>做一个不同的东西</ li>  
<li>} </ li>  
......并且编译器将知道如何解压缩到相应的机器代码中。  
在每个阶段，您都有使用前一层写入的工具;因此，Evolution可以通过在原始机器代码中编写一个简单的汇编程序，以及一个简单的编译器，用于组装中的基本编译语言，以及更复杂的编译器，用于基本语言中的更丰富的语言。  
### 回答 29
早期编程编程语言在纸上手写，组装和手动编译在纸上。  
当我10岁时，我父亲买了一台苹果二电脑。它没有外部存储，并配有32K RAM。启动时，您可以在十六进制代码中获取您在程序中键的机器语言shell。然后，您执行跳转指令以运行程序。关闭整个程序的电源消失。  
后来我们得到了一个AppleSoft Basic（由比尔盖茨写的）Daugtherboard卡，然后我写了基本计划。然后后来我们得到了5.25“软盘驱动器，可以将我的程序保存到软盘中。  
早期计划用汇编语言编写。没有文本编辑器和编译器，并且装配程序在纸上手写。然后我们在大表卡中使用查找表，其中包含十六进制映射到装配语言指令和ASCII代码，以将它们转换为机器代码。我仔细地在十六进制中输入了每一个字节的代码。如果出现错误，我会再次关闭计算机并重做。  
汇编语言源代码可以在报纸，书籍和杂志上找到。他们中的大多数都没有评论，评论或解释。我父亲有一个集合，我修改并花费时间手动将它们翻译成机器代码。  
我们的机器带有一个具有引导和shell的ROM。我可以通过使用我的EPROM芯片替换ROM芯片来编写自己的固件。在那些日子里，微芯片没有焊接到电路板上，我们可以轻松更换电路板上的任何芯片。可以从业余爱好商店购买EPROM编程套件。  
### 回答 30
很多好的答案和一些以某种方式错过了标记 - 以及一些非常迷人的答案。  
什么是第一个编程语言？这是你必须回答的第一个问题，因为这就是主要问题是如何措辞的。  
计算机的第一个编程是通过接线 - 没有机器语言，没有汇编语言，没有HLL（更高级别的语言）。  
一旦实现了存储器中的CPU指令的概念，就使用了机器语言。我用它在LGP-30计算机上，带硒鼓存储器，固态二极管和真空管。幸运的是，我能够在纸带上写下我的程序，并在一次一个地上没有切换它们。那是1960年。  
后来，我学会了Fortran，据说是第一个HLL。  
在两者之间，有汇编语言。现在，您可能会或可能不会认为这是一个真正的编程语言，但这是我们所拥有的一切。此外，在Fortran，Cobol或RPG中，一项伟大的众多编程项目 - 几乎远远超过唯一的HLL。  
因此，我写了许多数千行的汇编语言代码。我的第一个项目是1974年DEC的新雇用的主要程序员是创建从医疗实验室设备读取的软件并将数据转化为人类语言。该项目完全采用PDP-11汇编语言完成。这只是我随着时间的推移工作的大量组装语言之一。  
要使用汇编语言，您必须具有汇编程序（以及链接器和操作系统，但我们将跳过这些）。通常（我不知道第一个已经完成了），程序员将使用机器语言开发一个汇编程序，该汇编程序翻译了他们实现的汇编语言的子集。然后，可以将机器代码转换为该子集中的汇编语言。从那里，可以更有效地扩展汇编语言。这个过程被称为自动启动，一个术语在早期计算机世界中具有各种意义。  
Fortran编译器是用汇编语言编写的，将Fortran语句转化为汇编语言，然后由OS由OSS链接，加载和运行的汇编程序组装。  
宏的概念稍后有点。 IBM具有Map（宏汇编程序），该映射提供了将一系列汇编程序语句转换为您自己的伪代码中的单个语句。如果您（或不会），这是XML的前兆。  
一个名叫Bill Waite（我知道的研究生院）设计并写了他所谓的stage2，这是一种普遍的宏观语言，他将在Mag录像带上携带的国家携带到学院/大学的任何硬件上我们进行了他的研讨会/谈判。我在非常早期（序列号832）PDP-11上使用了这个工具，以创建实时语言，用于实验室使用，Miracl。我使这个侧面偏移表示HLL的单个路径不仅仅是一个路径。  
因此，HLL的路径曾经（在严格编译的日期中）从机器语言到汇编程序到HLL。解释的语言改变了这一切。在编程中有不止一种方法可以做任何事情。但是，你已经知道了，不是吗？  
### 回答 31
我理解这个问题的有趣感知的悖论。另一酷的东西是  
嗯，给定处理器的第一语言实际上是该特定处理器的指令集。所有抽象都在其中使用这些指令。所以c例如实际上与CPU与CPU不同。好吧，不是你写的高级语言，但实际上在CPU上执行的命令来执行执行您编程的操作所需的操作将会有所不同。这例如解释为什么您不能拍摄C编译器并在另一个CPU架构上使用。  
我理解这个问题的有趣感知的悖论。另一个很酷的东西是，想象一下，想要在给定的架构上实现C，你用C来做它:)。如何？好吧，首先说你，在CPU理解的低级语言中，写一个基本的C编译器。然后，您使用该编译器使用C的编写更全面和高效的编译器。这一切都变得非常荟萃:)  
### 回答 32
John Backus和他的团队在IBM 704中写了第一个FORTRAN编译器。第一个汇编程序是用机器代码编写的。如果您想了解为什么Fortran被发明，请尝试以汇编语言开发复杂系统。然后，就像那个IBM团队一样，您可能会受到启发，让您的生活更轻松。懒惰的规则。  
### 回答 33
<li> //让我们说机c </ li>  
它是用机器代码编写的。但你必须认为这首语言非常简单。你可以成像一系列这样的事件。这不是语言的真正历史，但如果你在今天开始。还要记住，从步骤1到5是几十年来，并拍摄了电脑也发展。  
<ol> //在3 param加载/存储指令集中添加。 //让我们说机器代码是4位OP，3个4位寄存器值添加R2，R3，R4  - > 0x0001 0001 0010 0011 </ OL>  
<li> //在3 param加载/存储指令集中添加。 </ Li>  
<li> //让我们说机器代码是4位OP，3个4位寄存器值</ li>  
<Li> Add R2，R3，R4  - > 0x0001 0001 0010 0011 </ Li>  
2.使用此基本汇编程序您拥有并开始编写可以跟踪名称或标签的汇编程序。当生成代码时，需要将这些问题解析为相对或绝对地址。现在，您开始拥有一个真正的汇编程序和链接器的开始。这些步骤导致开发Lex / Flex等工具。  
<ol> lable1：//在装配//测试r0中做某事，如果为true，则跳转到表。 JLE R0，标签</ OL>  
<Li> Lable1：</ Li>  
<li> //在组装</ li>中做某事  
<li> //测试r0并跳转到表（如果为true）。 </ Li>  
<li> jle r0，标签</ li>  
3.现在人们可以从汇编程序制作程序，也许他们开始使用解释的语言并开发基本。这些都很慢，但允许我们更轻松地编写复杂的代码。这些导致解析yacc / bison等语法和工具。  
4.最终他们希望从更高级别的语言中表现更好。因此，他们开始编写一个可以以基本语言拍摄语句的工具，并将这些语句转化为汇编程序代码。这个前端到您的汇编程序允许更复杂的代码。这个前端可以使用Flex和Bison来解释语言的语法。  
5.现在使用Set 4中的编译器并构建更好，更复杂的语言。在40-50年内重复这一步，你最终会在今天的位置。  
### 回答 34
语言演变中的幽灵乐趣部分是当它变得足够复杂，可以编译自己的编译器。一旦语言'B'变得复杂，足以编译一个简单的语言'c'，'c'的编译器被重写为'c'！  
这对语言来说是一件非常好的事情，它甚至生成了一个术语：吃自己的烹饪。  
### 回答 35
它不是鸡肉和蛋问题。当我们知道上帝（古代程序员）使用3D打印（机器代码，10101s）来生产第一个鸡蛋（汇编程序）时，解决这个问题是解决的第一个小鸡。（部件）  
### 回答 36
第一个编程语言是汇聚器。其中的第一个通过交换机寄存器处理。  
后来这些程序是从纸带，打击卡等加载的。  
例如。最初加载纸带的程序通过切换寄存器切换到系统中。我在2070年代在数字PDP 8 / E中使用了RIM（读取模式）加载器。查看：复古数字PDP-8计算机，您擅长阅读八大人或十六进制:)  
请参阅此视频，看看如何通过交换机寄存器编程PDP 11。  
### 回答 37
如果编程语言也以编程语言编写，则第一个编程语言如何写入？  
在有任何编程语言之前，计算机在二进制代码中编程。说明可能有名称，但电脑对他们有任何了解。  
第一个编程语言是汇编语言，使用指令的名称和其他名称进行待操纵的数量。汇编语言不使用数学表达式。每个语句指定运算符（例如添加，div，goto）及其操作数。将汇编语言转换为二进制代码的程序（以便计算机可以使用它）称为汇编程序。  
第一个汇编程序必须用二进制代码编写。然后可以用汇编语言编写更好的汇编器并与第一个汇编程序组装。然后，第一个高级语言的编译器（具有人员可读的语句的语句，然后用汇编语言编写并与汇编程序组装。当编译器存在时，它可用于为其他编程语言编写编译器。  
### 回答 38
如果编程语言也以编程语言编写，则第一个编程语言如何写入？  
有无尽的计算机处理器，无尽的方法来引导软件开发。从历史上看，第一步是在机器代码中编写一个简单的汇编程序。  
更具体地说，人们可以设计一个简单的汇编程序，本身写了一个版本，然后将该程序手工翻译成机器代码。然后可以在最新的汇编程序中写入改进的汇编程序。  
第一个改进是使用名称而不是绝对数字地址。然后它特别有助于添加宏。然后可以用汇编语言编写编译器或解释器，然后是编译语言中更好的编译器。  
或者，正如Alan Kay所指出的那样，由于Doug Engelbart为其在所有演示的母亲中最着名的系统中，可以创建一个用于编程的字节代码。  
我们在I-APL，Ltd。创建了一个便携式字节代码，用于在20世纪80年代在8位计算机上实现全ISO / ANSI APL。我们的开发人员Paul Chapman在那个字节代码中写了一个开发环境，他打电话给de。它是一个类似于类似的语言和字节代码翻译器的多窗筒编辑器。  
为任何微处理器提供用于该系统中使用的字节代码的汇编文件，使得可能在几乎任何计算机上运行我们的APL。保罗还在C中写了一个字节代码解释器，以便我们可以使用C编译器在任何16位或更大的计算机上运行它。  
字节代码也称为令牌线程。基于许多基于螺纹代码语言，最初使用地址线程。它比令牌线程更快地运行，但占用更多空间。最初的解释器的大小约为8k字节。对于I-APL，它在令牌线程中占用29k字节，地址线程不实用。 FORTH是一种在新处理器上引导的最简单的高级语言之一。  
另有APL称为APL68000，用68000汇编程序编写。 Lead Developer而不是重写整个翻译，而不是重写一个68000到PowerPC转换器（在APL中），允许APL68000运行PowerPC处理器的其他更改。该仿真器还被销往其他公司，让他们在Powermac的PowerPC中搬运他们自己的68000个软件。  
扭转了这个问题，我在龟艺术中写了一个图灵机仿真器。您可以在每份/糖实验室页面上的一台笔记本电脑上查看图形源代码和执行示例。  
活动/龟艺术/教程/龟艺术图灵  
用于图灵机的活动/龟艺术/教程/龟艺术代码  
我们还有一个龟艺术录音机，因此我的图灵机也有徽标代码。  
我的另一个项目，称你成为乌龟，意味着将学龄前儿童引导到他们的老师和彼此作为龟艺术口译员进行编程。  
### 回答 39
我去的大学里有一个独特的设置。由于我是一名学生，我不能给电脑室的关键，但是只要我没有离开或让任何人都在休假，他们就会让我留下来。  
所以我在我的处置时有一个IBM 360/30。老师给了我一个挑战，在Keypaned Boot皮带代码后写下并打击卡片甲板的打击卡上的绑定程序。  
我将IPL地址设置为00C，这是卡读卡器，在我开始工作之前花了几周的踪迹和错误。  
感谢Dan Mooney让我学习实时和教我PL / I，汇编者，COBOL和APL。  
我去的大学里有一个独特的设置。由于我是一名学生，我不能给电脑室的关键，但是只要我没有离开或让任何人都在休假，他们就会让我留下来。  
所以我在我的处置时有一个IBM 360/30。老师给了我一个挑战，在Keypaned Boot皮带代码后写下并打击卡片甲板的打击卡上的绑定程序。  
我将IPL地址设置为00C，这是卡读卡器，在我开始工作之前花了几周的踪迹和错误。  
感谢Dan Mooney让我学习实时和教我PL / I，汇编者，COBOL和APL。  
这导致了IBM大型机系统和应用程序编程和管理的31年职业，使我能够为我的家人提供...  
### 回答 40
计算机仍然在二进制中运行。开关可打开或关闭。  
第一台电子计算机通过开关墙壁上或关闭的垃圾横向编程。  
一旦程序不起作用并且验证所有交换机都在正确的上或关闭位置，他们开始分开开关并检查它们。他们发现昆虫的一个开关后面。这是世界上第一个电脑错误，从那时起我们称之为电脑故障错误。  
### 回答 41
1964年，政府计算机软件程序是用3个基本语言编写的SPS-1 SPS-2和Auto Coder，这些不是第一个在60年代初出现的RPG，但所有被编写的代码汇编程序使用机器代码使用命令的字符和两个机器地址位置之一。 IBM和Univac是两个最大的开发人员和稍后的GE / HONEYWELL开发了简单的编码器。在此之前，IBM 407使用用于逐行机器步骤的插头接线编程，并且仅限于基本计算和重新排列以打卡格式收到的数据，以重新排序将如何打印，大多数基本是80-80列表。机器代码如M for Move C，用于将B比较B分支到另一个程序位置，并且W在打印线上写入一行排列的字符。  
### 回答 42
在进行编程活动时，外部代理商通过CO  
根据我们认为的编程和第一个程序，可以有许多不同的答案。其他答案从现代电子计算机的角度来看，我将尝试呈现对受试者的广义治疗。  
通常作为活动编程涉及计算系统，该计算系统使用诸如晶体管的诸如电压的一些物理现象，硬盘驱动器的磁性，机械计算器的某种杠杆，DNA中的聚合物分子，等等  
在执行编程活动时，外部代理将愈合状态的集合传递给系统使用其他物理现象，例如按下按钮，发送电信号，化学突变状态等。  
有时，有一个变换层涉及。例如，按下按钮，导致电流的流动，这导致改变电子计算机上的晶体管的电压。或者，您可以在麦克风中讲一些东西，然后将那些声波转换为电流，然后设置晶体管的状态。或者，您可以在纸张上写出一组关于读取的神经元的一组说明，然后将其读取，然后逐步遵循指示步骤。事实上，当他在使用自己作为计算代理时，当他在纸上编写的国际象棋程序时，艾伦图灵做了类似的事情。  
第一个编程语言的定义现在取决于我们正在谈论的可编程宇宙的哪个部分和阶段，并且可以根据该域可用的许多物理现象之一编写。  
### 回答 43
您可以编写一个最小的语言，能够编译自己。从那里，你可以扩展你拥有的东西。  
要获得编译的最小语言，您可以使用另一个（历史上更多的原始）语言。  
在堆栈的底部，您首先将特定数字写入内存，即使是偶数。你仍然可以用这种方式编写一个简单的程序。  
遵循的所有语言开发是抽象的系统，非常慢，易于易于锻炼。  
### 回答 44
这必须是它。这是一个由Alan TING发明的有线电脑。  
艾伦图灵 - 维基百科  
但是，如果您正在谈论编程语言，当然它必须是二进制代码，除非您像唐纳德Knuth一样古怪，谁创建了他自己的教学基于小数的计算机，在这种情况下，您将以十进制和称为混合的语言编码。  
### 回答 45
当有人说，简而言之，答案是使用机器代码，甚至不是汇编程序，因为问题将被转换为写入的第一个汇编器的写入方式，答案是相同的：使用机器代码是相同的：  
机器代码是其设计师团队嵌入微处理器中的语言;机器代码中的程序必须存储在计算机的某种内存中，（并且与记住第一个数字计算机不是电子版本相关）。在电子计算机的开始时，不可用的电子存储器不可用，并且RAM由磁性（铁氧体）核心制成，当切割电源时，将失去其内容的内容，因此代码必须通常在某种ROM之间存储在某种ROM之间的会话之间穿孔纸带或穿孔卡（如今我们有硬盘和长期的不同电子记忆（ROM，RAM，PROM等），在数字计算机历史的开始时不可用）。  
您是否希望了解机器代码中的编程是什么？超过五十年前，我有特权在发展实时操作系统，数据收集和报告的发展，以及在1960年代的过去几年中由IC和离散组件制成的数字计算机的数字过程控制软件（CID-201-B非常类似于美国的流行PDP-8）。我们直接在与硬件开发人员共享的原型上运行，没有可用汇编程序或编程语言，因此，每天都要启动我们的工作，我们必须在计算机中输入短加载程序（使用opggle开关和按钮，没有十六进制由其他一些贡献者表示的键盘可用）可以使用纸磁带阅读器读取穿孔带;在每个会话结束时，我们必须打出一个新的纸带，其中包括在会话期间包括更改和添加到软件。机器代码中的编程虽然效率令人畏惧：在CID-201-B的情况下，指令曲目仅由八个基本指令组成，并且您必须使用该软件的那种有限的指令，以便您可以想象如果没有数以千计的指示，则需要数百个浮点数的简单总和！  
### 回答 46
这里有很好的答案，除了计算机语言是图层之上的层之外，我没有太多添加。不同的范式不同的东西，并且有很简单的语言可以只用几行封装几种复杂的概念。  
我认为，更少的代码行意味着对我所看到的大多数语言的思考更多的时间，你必须在跑步前思考和思考和设计这些东西。相反的语言更详细，不需要这么多的心理努力，你可以更快地原型，这就是为什么要如此喜欢的语言如此流行，你可以在结束时写一个像食谱一样的问题分辨率。我们可以解决像食谱等几个问题，但是，你不能出现一些问题。那些与机器学习，解析，并发或实时程序等有关。  
最后，语言只是几层抽象，你仍然运行旧的好0和1的。  
### 回答 47
第一个计算由程序进行硬线程编程。  
然后通过翻转开关和转动旋钮将机器语言（1s和0s）直接输入到更多1s和0s指定的位置。  
来自hecidecimal中的终端输入的能力。  
潘卡卡在早期的时候......他们在有机器排序，计数和独立整理的机器时，他们预测的计算机。  
然后来汇编语言。随着装配，您可以使用STO和添加代码而不是数字。  
他们写了更高的级别语言...宏装配[AutoDoder]，以及Fortran（Fortran）和Cobol。  
第一个计算由程序进行硬线程编程。  
然后通过翻转开关和转动旋钮将机器语言（1s和0s）直接输入到更多1s和0s指定的位置。  
来自hecidecimal中的终端输入的能力。  
潘卡卡在早期的时候......他们在有机器排序，计数和独立整理的机器时，他们预测的计算机。  
然后来汇编语言。随着装配，您可以使用STO和添加代码而不是数字。  
他们写了更高的级别语言...宏装配[AutoDoder]，以及Fortran（Fortran）和Cobol。长期以来，所有更高级别的语言都以某种形式用汇编代码编写。  
### 回答 48
简而言之，C之前的大多数计算机语言最初在目标机器的本机汇编程序中写入。少数后来重新开发到能够抚养自己;Fortran可能是第一个定期的自主托管语言。许多高级编译语言和一些解释语言，这些天是用C，或C和C ++编写的。大多数GNU编译器套件和基于LLVM的语言都写于C. Perl，Python，Python和Google节点JavaScript引擎都是在C中写入的。  
### 回答 49
首先是用绝对机器语言编写的（使用开关和灯光），然后穿孔读卡器，然后来了。只有在那个更高级别的语言才会创建（例如COBOL，GOTRAN，FORTRAN））  
### 回答 50
它是通过自动启动完成的。首先，这个问题非常有趣，因为很多人不询问如何在没有编译器编译它时首次编制编程语言。  
Bootstrapp是简单的想法，在那里在汇编中开发初始，简单的编译器，然后您使用这个简单的编译器来编译您的目标语言的完全成熟的编译器（您的目标语言的子集）。  
### 回答 51
好的，这实际上真的很容易。设计CPU或微控制器的人是编写初始语言的人。您可能通常不使用微控制器或CPU，但如果您有家用电脑/笔记本电脑，你携带大约5个。CPU绝对有一种由其建设者写的语言。语言真的只是一个表格列表的表格和每个数字都使CPU的解释 - 微妙的记录是理解如何使用这些表并了解该表来编程计算机的工作，这就是您找到第一种语言的地方。例如，请查看网页上设置的此指令：  
8051指令集  
详细说明…  
使机器成为计算机的事情，要说一般地说，它能够读取指令，执行它，并循环执行此操作。这涉及一些电路来存储指令列表，按钮进入有关的说明和启动执行循环的按钮。可以将输出放在LED上的东西，如LED，但这并不关键。您拥有此电路设置，您可以使用该书以及他们的手提，你有一种语言  
您只需编写该语言的程序 - 其中包含一个指令列表及其含义，在2列表中。您读取含义列，弄清楚如何排列一行中的几条指令以完成任何内容您想在正确的顺序写下相应的指令，在计算机中输入它们，按下GO按钮并查看它。这一切都可以完成，今天，取决于如何胖你的手指是，指令集是多大的指令。基机语言有简单的说明，如：  
将数字5放入显示屏上显示的区域  
将存储器地址2中的值添加到存储在加法器中的值  
将加法器中的值移动到内存地址5中  
展开3条说明  
跳回10条指令  
......这些你可以做大多数计算机功能（有一个整个数学理论展示了你可以计算的内容，给定一组最小功能）.i让这些语言进入英语上面，但你进入的实际说明微码级别在二进制中 - 因此您可以实际上输入以下数字集列表：  
00011001 0101 1110  
（这可能意味着，将数字5放在内存区域14中）  
00001110 1101  
（这可能意味着，将内存区域13的内容添加到加法器）  
......  
这就是计算机将在其指令中看到的 - 在此级别的情况下，您在硬件上，您真的无法更低，没有一点电路/微电子/微控制器课程。微控制器/ CPU的设计者决定了什么这些数字使机器能够。它可以达到你的眼球是一个更长的解释。所以我猜这是第一语言。你有了这些说明的第一语言，你可以开始考虑写作为你的程序编写程序的编写程序更高级别的语言。有一个更高级别语言的原因，但是一个原因是避免重复自己很多。如果你知道这样做是常用的，那么这样的循环：  
------  
Loop1：  
将数字10放入内存区域7中  
（也许在二进制中，这是00111001 1010 0111等）  
将内存区域4的内容移动到加法器中  
将内存区域5的内容添加到加法器中  
将加法器的内容移动到内存区域6  
将内存区域7的内容移动到加法器中  
从加法器中减去1  
将加法器的内容移动到内存区域7中  
如果内存区域7大于0，则跳转到标签循环1  
结尾  
（本质上，这表明，每次通过添加4和5并在6中存储结果  
那么你可以把它写像在记事本，你想用的时候就一个模板，你可以剪切和粘贴... ...。或。你可以在一个文件相同的文字存储连接到您的微型电脑的磁盘上，并创建一个小程序，通过一个文本文件中读取无论在哪里，找到这个词for循环它插入文本instead.So那么你就写在它的文本for循环的文本文件，你就可以运行你的程序，你会得到一个文本凭借上面的循环代码的文件。  
### 回答 52
答案对您的问题的答案是，由于两个不同的真实和虚假状态表示的数字系统的本质，这些状态没有写入它的第一个编程语言，这些状态代表了也称为机器语言或语言低电平的机器代码。  
第一个数字机器是这种语言的程序，后来系统变得复杂，并且程序更难以详细阐述，感知和调试，因此需要将某些命令组分组，这是通过十六进制和八进制语言完成的。虽然程序已经简化了这一分组在一段时间内变得无效，但在一段时间后，已经进行了更多的集群，并且已经出现了汇编语言，这也被认为是低水平的linguam。此后还被分组，以使程序更接近人类语言，而这些课程将翻译成机器语言。  
总结编程语言的进化线：  
机器语言 - >通过数字系统的性质来源  
语言装配（低级） - >位分组（机器语言）  
高级语言（Fortran，C，Pascal等） - >其他分组。  
### 回答 53
让我们说我做了自己的处理器。  
我还制作了一款手册，描述了我的处理器可以做的事情。  
我的处理器可以添加，缓和乘以并跳过内存  
在手册中，您可以找到如何执行这些操作。例如，如果您希望处理器添加2个数字，则OPCode添加处理器是01101101101110。  
当我的处理器在内存中看到此操作码时，它将将下一个2个字节添加在一起并将结果存储在下一个。  
您可以使用这些操作码开始编写程序。但我建议你的第一个程序是翻译。找到帮助您remebode opcode是doin的单词  
让我们说我做了自己的处理器。  
我还制作了一款手册，描述了我的处理器可以做的事情。  
我的处理器可以添加，缓和乘以并跳过内存  
在手册中，您可以找到如何执行这些操作。例如，如果您希望处理器添加2个数字，则OPCode添加处理器是01101101101110。  
当我的处理器在内存中看到此操作码时，它将将下一个2个字节添加在一起并将结果存储在下一个。  
您可以使用这些操作码开始编写程序。但我建议你的第一个程序是翻译。找到帮助您remebode从手动进行操作的单词，并使该单词产生操作码。例如，对于添加的操作，单词添加很容易记住，它应该被转换为01101101101110。  
为每个操作码进行此操作，并使您的第一个程序将单词转换为操作码，因此您可以在下次编写程序更轻松。  
### 回答 54
通过从机器代码开始，基本思想由墓碑图 - 维基百科代表。  
### 回答 55
这是乌龟一路下来。  
此外，大会：  
编程语言的历史 - 维基百科  
汇编语言 - 维基百科  
### 回答 56
我假设你的问题是关于现在写的自托管语言。在这种情况下，首先用C写入C，然后在语言中建立了一定程度的稳定性之后，C被翻译成了。  
Alan Kay提供了对一些早期编程语言的详细讨论自己，我推迟了他对对您的问题解释的专业知识。  
### 回答 57
答案是：编写编程语言的语言始终是编程语言本身。  
编辑：  
维基：  
编程语言是一种正式的语言，指定可用于产生各种输出的一组指令。编程语言通常由计算机的说明组成。编程语言可用于创建实现特定算法的程序。  
这是关于主题上下文中的任何内容。第一语言是用于表达技术规范条件的人类语言。首先，设计和技术D的芯片  
答案是：编写编程语言的语言始终是编程语言本身。  
编辑：  
维基：  
编程语言是一种正式的语言，指定可用于产生各种输出的一组指令。编程语言通常由计算机的说明组成。编程语言可用于创建实现特定算法的程序。  
这是关于主题上下文中的任何内容。第一语言是用于表达技术规范条件的人类语言。首先，设计的芯片和用于表达这种任务的技术描述。芯片编程的语言出生在芯片中，这意味着作为技术规范语言的人类语言是所有编程语言（IMHO）的母语。  
### 回答 58
作为Clive和其他人表示，机器语言（位，字节，十六进制）。然后是一个汇编程序。我的第一份作业我设计了一个基于8085处理器的系统，如一个Clive描述。我花了明年在汇编程序中编程它。我正在写一款串行（RS-232 / RS-485）协议处理器;大约40k字节的程序和20k字节的数据。没有C编译器（或任何编译器，那么它）。下一个系统我设计了我确保它有一个C共同可用。我仍然不得不编写一些汇编程序来获取C要运行它，但这很容易。  
### 回答 59
汇编语言。低于此，您正在处理1和0（二进制），这是机器语言。  
### 回答 60
这正是我开始的地方。我曾经做过的第一个编码在380z的研究机器上。英国学校市场特有的一台机器，但基于旧Z80 CPU。此计算机具有“前面板”，该计算机是一个软件等效于全尺寸计算机上的开关面板。这允许阅读和写入存储器并能够以单步或全速启动任何特定位置执行的CPU。这就是我学会了编程计算机的方式。  
### 回答 61
将第一个程序进入早期内存的计算机涉及在控制台上翻转开关，或者如果计算机能够读取手动准备的打卡卡。一旦监视器或编译器处于内存中，它可以用于读取源程序并将其编译为计算机语言。它有助于铁氧体核心存储器当时是一种常见的技术，当电源关闭时，不会擦除这种类型的存储器。  
### 回答 62
这不是。第一个编程语言不是真正的编程语言，它们都是一种简单的数学语言 - 有点像一个可编程计算器。然后我们有汇编程序的概念 - 用文本机代码编写，增加了复杂性，然后我们有简单的编译器。  
### 回答 63
在70年代末，80年代早期我正在编写汇编语言的代码，为大型电动实用程序。汇编器是在房子里开发的。组装汇编器是相当常见的。每当添加新的指令时，第一个迭代是用机器代码编写的，该版本用于组装下一个版本的汇编程序。狂欢的思绪意识到我们今天使用的语言并认为所有人都从同一个“种子”开始。  
### 回答 64
阿达洛夫莱斯是英国数学家。她记发表意在通过机器执行的第一算法。  
参考：维基百科  
我要说的是，最早的现代计算机语言是真空管的创建。随着管我们可以说“真”或“假”，甚至代表二进制通信的1和0。  
管的在早期计算机中使用的类型被称为一个三极管和在1906年由Lee德福雷斯特发明的，无线电的父亲（马可尼无线电通信的父亲）。试管后来改用晶体管。  
当我开始了我的电子学习  
阿达洛夫莱斯是英国数学家。她记发表意在通过机器执行的第一算法。  
参考：维基百科  
我要说的是，最早的现代计算机语言是真空管的创建。随着管我们可以说“真”或“假”，甚至代表二进制通信的1和0。  
管的在早期计算机中使用的类型被称为一个三极管和在1906年由Lee德福雷斯特发明的，无线电的父亲（马可尼无线电通信的父亲）。试管后来改用晶体管。  
当我开始我的电子研究，管仍然在大量使用，所以我们必须学会如何用管和并行地与超现代设计的晶体管。我从来没有使用管道在现实生活中（除吉他放大器），而是通过训练在经历给了我一个点从地球上消失了几年后的视图。  
### 回答 65
如果您曾经编程过Altair 8080，则痛苦地意识到机器代码的工作原理。 Altair要求您在运行它之前将机器代码切换到内存（字面意思 - 使用Toggle开关）。第一代汇编程序是用机器代码编写的，只是为了使这个过程更痛苦。第一个Gen编译器是在装配中编写的。现代编译器在第一次通过时生成伪代码，然后将p代码转​​换为最终通行证的机器代码，因此这是一个多级过程。可以发明新语言，该语言共享相同的编译器后端，只要它们会向前生成相同的P代码。  
### 回答 66
史蒂文J Owens回答，因为每个编程语言是在P中开发的  
其他几个答案是良好的读物，并提出了有趣的更高级别的概念，包括Alan Kay（当然），也是克莱夫托马斯和哈利凯勒，以及其他几个。  
一些答案触摸了您的问题的真实答案，这是首次编程，在手动改变接线上，然后从那里逐渐发展，连续增加更多的编程便利。  
真正早期的故事很复杂。我在回答类似的问题时刺伤了：  
STEVEN J OWENS回答，因为每个编程语言都是以前现有的语言开发的。哪个是第一个编程语言，它是如何发展的？  
### 回答 67
所有人类可读（又名高级别）的语言写被编译成机器级指令的（第三代语言）可以写在俗称汇编语言第二代语言。  
汇编语言 - 维基百科  
这样的语言对应，他们为开发特定的计算机架构的指令集。因此，第一个高级语言有一定的架构来开发，然后重新编码为其他计算机体系结构。 （一旦第一语言，设计并实现这个过程比较简单。）  
今天，许多语言编译成所谓字节码，这是一个抽象层，机器允许跨机器语言的近乎完美的便携性上运行。  
### 回答 68
第一个高级语言是Plankalkül，由康拉德楚泽在1942年和1945年之间创造了...第一个商用语言是FORTRAN（公式翻译）;由约翰·巴克斯，IBM的工作人员于1956年开发的（第一手动出现在1956年，但在1954年首先开发）。  
Plankalkül引起了比较，APL和关系代数。它包括赋值语句，子程序，条件语句，迭代，浮点运算，数组，层次记录结构，断言，异常处理等先进功能，如以目标为导向的执行。所述Plankalkül提供一种数据结构被称为广义图（verallgemeinerter图形），它可以用来表示几何结构。  
Plankalkül共享使用多行具有1879 Freges概念文字（处理数理逻辑）是一种特别的符号。  
在Plankalkül的一些特点：  
数据类型  
在Plankalkül唯一原始数据类型是单个比特，由S0表示。进一步的数据类型可以从这些被建立起来。  
术语  
楚泽称为单个节目的Rechenplan（即计算计划），并在1944年，他已经预见到要读出，然后自动翻译程序的数学公式转换为机器可读的打孔胶片的设备 - 一个他称之为Planfertigungsgerät设备（即计划施工设备）。  
例子  
原来符号是二维的。对于20世纪90年代以后的实施，线性符号被开发。  
下面的示例示出的程序（以线性的转录），其通过调用函数MAX3计算最大三个变量：  
<OL> P1 MAX3（V0 [：8.0]，V1 [：8.0]，V2 [：8.0]）→R0 [：8.0]最大（V0 [：8.0]，V1 [：8.0]）→Z1 [：8.0]最大（Z1 [：8.0]，V2 [：8.0]）→R0 [：8.0] END P2 MAX（V0 [：8.0]，V1 [：8.0]）→R0 [：8.0] V0 [：8.0]→Z1 [ ：8.0]（Z1 [：8.0] <V1 [：8.0]）→V1 [：8.0]→Z1 [：8.0] Z1 [：8.0]→R0 [：8.0] END </醇>  
<LI> P1 MAX3（V0 [：8.0]，V1 [：8.0]，V2 [：8.0]）→R0 [：8.0] </ LI>  
<LI> MAX（V0 [：8.0]，V1 [：8.0]）→Z1 [：8.0] </ LI>  
<LI> MAX（Z1 [：8.0]，V2 [：8.0]）→R0 [：8.0] </ LI>  
<LI> END </ LI>  
<LI> P2 MAX（V0 [：8.0]，V1 [：8.0]）→R0 [：8.0] </ LI>  
<LI> V0 [：8.0]→Z1 [：8.0] </ LI>  
<LI>（Z1 [：8.0] <V1 [：8.0]）→V1 [：8.0]→Z1 [：8.0] </ LI>  
<LI> Z1 [：8.0]→R0 [：8.0] </ LI>  
<LI> END </ LI>  
### 回答 69
如果鸡蛋来自鸡肉，鸡肉来自鸡蛋，第一个鸡蛋是如何铺设的，第一个鸡嘴是如何孵出的？  
### 回答 70
如果您还将装配数为编程语言，则第一个装配编译器被编码为二进制CPU指令，通过八进制或十六进制代码。去过也做过。从根本上讲，在更简单的CPU上并不难，特别是随着组装本身非常接近实际的CPU指令。  
还记得旧的商用电脑吗？我通过数据编码CPU指令和循环戳。  
### 回答 71
简短的答案是第一个简单的汇编程序必须用机器代码编写。  
然后，您可以启动使用该汇编程序写入更复杂的汇编程序的引导过程。然后使用它来编写一个更复杂的汇编程序，依此类推。最终汇编程序对于以汇编语言编写的第一个编译器将足够强大。然后您对编译器重复相同的引导过程。  
### 回答 72
一种编程语言不是写在其他编程语言。  
您在这里混淆了编程语言和它的实现。编程语言无非是一组的语法规则。但是，这种转换新的语言把它写在另一种语言，但有时在同一种语言或低级语言已经理解语言的实现（编译器，解释器，transpiler等）。  
低层次的编程语言是有着非常密切的紧身衣将硬件平台上运行的编程语言。大会实例提供的编程语言和二进制指令之间的直接关联。  
没有什么可以阻止从编写直接二进制代码的人。其实仍然是一个非常普遍的做法，最安全漏洞涉及到执行代码就是这样做的 - 直接使用二进制代码（如果你喜欢的1和0的字符串）。  
### 回答 73
软件最初是用机器代码或最佳装配编写的。Fortran被声称是编译源代码的第一语言。（装配编写的编译器）  
### 回答 74
在这种情况下，我相信第一语言是人类，更准确的是......数学语言，你可以用艾伦图灵和他的机器的图灵等待这个数学概念的身体表达。我推荐这本书更多信息由George Dyson的Cathed大教堂  
### 回答 75
最简单的编程语言，汇编语言的简单的版本中，可以在目标处理器的机器代码直接编程。那么这可以用来程序较为复杂语言，如B.  
许多新的语言都有，因为他们的定义，写在自己的简化子集的编译器的一部分。其他人，如BCPL，历史上非常重要的语言，编译成中间语言（在此情况下，O型码），这是很容易汇编关于目标的架构，而编译撑条的第一步骤相同的无处不在。 GNU编译器集使用各种中间语言来简化从背面（优化和代码生成）的便携性和单独的前端（词汇和语义分析和解析）。人们构建编译器所调用的第一手动执行，然后编译完整的编译器引导语言的简化版本，这种做法，和它无数的版本存在。  
中间代码有时不是在所有编译而是由虚拟机解释 - 程序仿真另一计算机系统。这方面的一个例子无处不在的Java字节码和JVM中执行它。这两种方法可以结合起来 -  JVM的大多数实现能够编译字节码下到底层计算机的本地机器代码。  
### 回答 76
在机器代码中。回到所有可用的那一天。但是一旦你得到你的第一级高级编程兰格，写作下一个语言变得更容易，那么接下来就会比这更容易。  
如今，许多编译器是用他们编纂的语言编写的。  
### 回答 77
所有编程语言都在长工具链的末尾转换为原始的好旧0和1的实际在机器上执行的1。简单地，因为这是计算机理解的唯一垃圾。  
因此，要回答您的问题，第一个编程垃圾箱实际上是用手写入机器的二进制指令。  
### 回答 78
装配/机器代码。即使是现在人们甚至人们有时候会引导小玩具编译器或口译员。通常的变体最大限度是编译器批评者的仪式仪式。  
### 回答 79
您需要了解电脑的历史，真正理解了答案。  
第一计算机语言实际上手动插入导线插入在该硬件存储器位置。这些硬件路径是计算机上的第一个机器语言，以及它实际上更多的是一个计算器：  
但是，简单地说，编译器，或在某些情况下的解释，仍然必须知道该架构的机器语言并翻译该代码。这就是为什么你不能把从MAC的东西，并把它在Windows机器，或大型机等上，而不进行重新编译或加载不同的解释。  
### 回答 80
你做到这一点的方法是定义一个编程语言L，并定义语言的一个子集是LS，其中LS足以写一个翻译在L到roduce机器代码的所有语句，现在可以执行的翻译。此转换是一个编译器，并称之为第一个编译器C0。  
你将不得不用手C0翻译成机器代码。 C0的转换是相当低效的，你真的不关心，你只是希望它是正确的。  
现在，您可以编写使用所有升，而不仅仅是子集LS语言L的编译器。你写这一个比C0更有效，所以你把它叫做C1。  
现在，编译使用从编译器C0手创建机器代码编译器C1。  
现在你已经翻译（编译）C1为机器代码，让呼叫T1，你再使用更高效的T1编译器来编译C1为机器码M1。现在，你可以写一个优化的编译器C2和使用M1将其编译为M2。  
现在你可以用语言L写其他的编译器。  
### 回答 81
在70年代我建了一个微处理器又名电脑脱销芯片和处理器芯片。使用记录的机器指令，我手动编程的ROM运行基本操作系统，响应键盘，并把字符在屏幕上。然后我变得更加增强，从盒式磁带读出器/写入器加载一个程序。这个程序读取简单的机器汇编语言和放出来的代码。然后我写了从汇编语言Pascal语言解释器。  
这就是所谓的自举。第一程序的位手动，则机器代码从，然后从该语言。每一步都变得更加复杂。  
在短短的几个月我从单纯的芯片了，一个算法语言，不幸的是我从来没有商业化。但许多美国书呆子做到了。这很有趣，当时的情况。  
### 回答 82
代码机子程序。那些是第一个。当您编译时（不在今天所谓的编程语言）时，它们会像乐高粘合一样使用那些机器代码子程序。  
### 回答 83
虽然任务听起来吓人，存在的数字计算机出现之前一直是各地的形式语言，自动机和编译研究的机构。  
通过提供这些原理可以在一个步骤中通过步骤和系统的方式被应用，以除去歧义在翻译和做进一步优化。  
在一个低的水平的语言和一种高级语言用两组符号提供，抽象和翻译可使用的原理来创建。这种翻译自动机基本上是一个编译器（虽然已经可执行代码更准确的术语编译器编译的部分）。然后，进一步进行理论来简单的编译器，我们可以有交叉编译器和编译器引导。  
参考：  
### 回答 84
我假设你在谈论语言引导。您可以在装配或机器代码中编写编译器。它甚至不必支持您语言的所有功能。然后，您可以使用那个糟糕和不完整的编译器，您可以使用带有语言本身的新编译器。在一些迭代之后，您可以拥有您的语言的完整编译器并优化。  
### 回答 85
在技术中，查找0s和1s，甚至计算器的历史。  
原始编程，生物编程，是未知的。您的RNA和DNA存储信息的方式或细胞的分裂或风化和元素的基本普括，以及彼此反应的是什么。那是未知的  
### 回答 86
我也有类似的经验，安德鲁（下图）。  
我是在一个小团队约5工程师/数学家的客人工程师。  
他们只是做了同样的事情。  
配置：键盘，英特尔8085 CPU，RAM没有EPROM那个时候呢。  
然后才能够重新运行（无论小程序是写在机器代码），他们进一步发展与I / O =纸阅读机/穿孔（！），小自制OS（加上org）。  
有在布达佩斯，匈牙利70年代那个时候没有任何显示设备。  
电脑主机使用IBM打字机控制台。我找不到高科技设备的图片。磁头总成是一个特殊的，全球SHA  
我也有类似的经验，安德鲁（下图）。  
我是在一个小团队约5工程师/数学家的客人工程师。  
他们只是做了同样的事情。  
配置：键盘，英特尔8085 CPU，RAM没有EPROM那个时候呢。  
然后才能够重新运行（无论小程序是写在机器代码），他们进一步发展与I / O =纸阅读机/穿孔（！），小自制OS（加上org）。  
有在布达佩斯，匈牙利70年代那个时候没有任何显示设备。  
电脑主机使用IBM打字机控制台。我找不到高科技设备的图片。头部组件是一个特殊的，地球形啄与表面字符，移动在所有方向上，并推动一黑色带紧贴纸张。  
这是在约（思（？）10-12 CHR /秒那个时候从IBM计算机最快的打字机？  
那些日子！  
有人认为IBM打字机控制台的照片吗？  
### 回答 87
初始问题不是您没有语言，初始问题是，如果您在装配中编写程序，则您没有计算机运行它。  
因此，最初人是将初始程序编译为机器语言的计算机。一旦您拥有一个非常基本的系统，并且您用手编译的非常基本的汇编程序，您可以使用此汇编程序扩展。  
### 回答 88
用技术在很久以前遥远的地方，人们会写出来的代码在纸上，手动“编译”成位，并手动将这些位加载到电脑。而这对于存储内存的计算机的第一个编程语言的编译器是如何创建的。  
当我在高中的在70年代初，我们有一个PDP8/ E。我频繁切换开关手动加载到运行的程序（通常是装载器）所必需的确切位。  
该视频演示了如何一个程序可以用在PDP8/ I开关来加载。  
### 回答 89
它从硬件开始。处理器可以在指定的序列中旋转和关闭的本身内的数十亿个开关/晶体管。以特定方式布置的开关可以执行特定的，逻辑操作，例如，或不等等，并且它们被称为逻辑门。逻辑，或者，而不是盖茨将暂时覆盖。逻辑门是为所有计算机构建数字电路的基本构建块。与它们的设计电路就像编程b ...  
### 回答 90
引导一个编译器的另一个有趣的案例是Pascal编译器。帕斯卡斯曾经在埃德人民受欢迎，因为它是一种相当清洁的程序语言。我为学校教学高（对不起，当时）的级别语言。  
plaform是一个罕见的（王）。不用担心：有一个帕斯卡的帕斯卡编译器完全是帕斯卡尔。如果您有一个Pascal编译器，您可以编译Pascal编译器，然后您有一个。  
现在使这项工作的诀窍在于Pascal编译器编制了Pascal进入P代码，这是一个机器独立的较低级别语言，接近实际的机器语言。 Pascal编译器的P代码可以由任何工作Pascal编译器制作，并提供所得的P码。您需要执行的唯一步骤是为P代码编写一个解释器，或者将P代码编译为您的平台上的一些支持的语言。  
我用后者在短时间内获得学校大型机克隆的帕斯卡。  
### 回答 91
唯一的接口“语言”是机器代码。不是装配 - 机器代码。机器语言的字节。虽然很短暂，但它在十六进制中编写了微小的节目，并让他们展示东西，但快速脱落。所以，当然，这是70年代，而不是60年代早期，下一个目标是使它理解一个编程语言 - 所选的一个被解释的基本。我们发现了一篇日本人（我认为）发布的文件，该文件有一个完整的基本解释器，用于机器代码中列出的处理器。我仍然记得有趣的是，纸质标题下面是一条舌头脸颊线，读过Copyleft，保留所有错误。 （编辑 - 有人找到了原始文章。李晨王于1976年发表了他的小型基础，他是中国人，而不是日本人。李晨王维基百科）  
我键入整个程序，并将其刻录为支持系统支持的EPROM（可能是8或16千字节）。我一定是正确的，因为从那时起就可以与微处理器以基本交谈。  
因此，对您的问题的一个可能的答案：通过在机器语言中实现它，可以将其实施，手动编码每个字节，并将其刻录到EPROM（或其他可编程存储器设备）中来实现第一个编程语言。  
### 回答 92
用真空管。  
任何计算机的最低级别语言都是其机器指令集。使用硬件创建。对于最早的计算机，那种语言是他们唯一的语言。  
### 回答 93
第一个裸露的语言版本是用另一种现有语言编写的语言（例如fortran，cobol，汇编程序）。下一个版本可以用语言本身写入，假设它在新语言中更容易/更好/可行。  
### 回答 94
最早的编程语言环境是用汇编语言编写的。实际上，泄漏到更高级别语言的一些机器级内容仍然在LISP中留下了回声（汽车是内容地址寄存器，CDR是内容递减寄存器）。  
因此，Fortran的最早语言往往非常接近硬件，编程语言的工作主要是为了掩盖计算机的繁忙工作。因此，Fortran名称对于公式翻译是短暂的。 FORTRAN翻译公式成机器语言。不受现代标准的兴趣。  
Lisp有点不同，因为Lisp基本上是兰姆达微积分的表示，它将在真实机器上执行。计算机科学的早期工作与证明不同的抽象机型相同。因此，早期的计算机科学家展示了Lambda微积分中的任何表达式如何转换为图定机器，以及如何将任何图灵机程序转换为λ结石表达式。如果您可以显示两种方式，则两个抽象模型相当。真正的机器看起来很像一类称为无限寄存器机的抽象机器。因此，McCarthy能够从Lambda微积分和无限的登记机等同的证据上灰尘，这给了如何为真实机器编写Lisp系统的很好的路线图。  
在下一代更复杂的语言中应用的相同基本模具，除了正式语言的飞跃。乔姆斯基和他的继任者在某些机器和某些类别的语法/语言之间表现出了二元性。例如推动自动机和上下文自由语法。通过这种等价，如果您可以描述语法的生产规则，可以自动化对应于每个生产规则的机器的结构。这允许语言设计师将编译器分成微小的隔离部分。这些碎片足够小，如果您知道要执行的物理机器，您可以提出实施该一部分语言所需的一点点机器代码。所以在这一点上，你正在以更高级别的语言编写大多数编译器，但最终阶段（称为代码生成）仍然是机器语言。  
### 回答 95
一个相关但不同的答案：  
早在上世纪80年代初，我们聘请了一位盲人软件开发人员。他是我们最好的之一。他是惊人的，是迄今为止在部门最快的打字员。  
他用一个苹果] [机器与文本到语音外设聊得如此之快，这是不知所云给任何人，用一组自定义的缩写来加速他的工作。对这个问题的关系是，他编程的苹果] [的二进制机器代码6502微处理器，这是他记住，书面把它变成一个终端的整个程序。这使他能够连入我们的系统来开发我们的软件（当时每个人都使用的终端到系统）。  
他的主要教训是，失明没有定义他。经过半打年他离开，去麻省理工学院攻读博士学位的数学。祝你好运，卡尔，无论你在哪里！  
### 回答 96
我们曾经在我们的高中拥有PDP-8（数字设备公司小型计算机）。它捐了。  
该计算机有一个引导序列。  
前面板有17个开关。  
引导序列是1和0，每行16的列表。  
要启动计算机，您读取了1和0的读数，并将开关定位到匹配 -  ON为1，关闭为0。  
在正确的位置处有开关后，将第17个开关从OFF转移到ON，并将16位字加载到内存中。猜猜第17个开关被称为什么？ （提示：负载开关）  
在加载所有启动指令之后，  
我们曾经在我们的高中拥有PDP-8（数字设备公司小型计算机）。它捐了。  
该计算机有一个引导序列。  
前面板有17个开关。  
引导序列是1和0，每行16的列表。  
要启动计算机，您读取了1和0的读数，并将开关定位到匹配 -  ON为1，关闭为0。  
在正确的位置处有开关后，将第17个开关从OFF转移到ON，并将16位字加载到内存中。猜猜第17个开关被称为什么？ （提示：负载开关）  
何时加载所有启动指令后，有一个第18个开关，称为运行 - 猜猜是什么？  
现在，从40年前从内存中都是从内存中所以我可能错过了一些细节。  
答：这就是字面上的，一路下来。  
### 回答 97
即貌似看起来像一个典型的鸡和蛋的情况非常有趣的问题。幸运的是，答案很简单，因为每台机器（电脑）设计，并配有自己的指令集，这实际上是计算机的机器代码的事实。  
本机代码是你唯一需要的，你不需要任何东西。既然是太痛苦和低效与机器代码的工作，也总是通过映射所述机器代码指令到更可读的和有意义的助记符，其有效地是在计算机或处理器的汇编语言的方式创建的抽象级别。  
因此，每一台计算机都有自己的汇编语言和第一个高级语言的编译器最初，我认为这是美好的旧的Fortran，我开始用，必须在计算机汇编语言编写的编程。  
### 回答 98
后来人们意识到它的如此之低的水平，它的很多工作  
机指令是（或至少曾经是）数值，当破碎成比特切换部件，如果CPU逻辑或关闭。正是因为霍尔瑞斯打孔卡织布机一样。  
现在，多数民众赞成罚款的计算机，但我们人类有时候是有点厚。因此，我们制定了这告诉我们，价值做了什么话。现在我们有一个程序，有人辛辛苦苦写了翻译的那些话（助记符）到nbers的机器。他们翻译的助记符自己。香港专业教育学院这样做了很多很多的场合。  
后来人们意识到它的如此之低的水平，它的大量的工作和你最终做这样的循环完全一样的东西所有的时间。所以高级语言进行设计，使通用的流程更容易为人类应对。这些第一编译器会在较低的语言被编码，甚至手转换为机器代码表示。但是，一旦它的工作原理，你再与自己编译的编译器。这就是所谓的自举。  
### 回答 99
简短答案：汇编语言  
伦答：等待某人写作。直到那时，满足于此答案所以（第一个编译器是如何制作的？）。  
### 回答 100
如果您不熟悉图灵机，请谷歌和我们  
简短的答案：它不是。问题是猜测。  
更长的答案：如果抹肉夹头夹木子，木质钟就会夹住多少木头？  
一个不需要“编程语言”为任何其他计算机语言编写“编译器”。我的编译器为我自己的编程语言写在牙签 - 小件木头，正确？我会一次喂食牙签，进入我的牙签电脑（申请专利）。第一个编程语言被写为一系列牙签。伍德克是CPU。  
如果您不熟悉图灵机，请谷歌为此并使用它作为模型。  
### 回答 101
有不同级别的编码软件。最低的形式是直接机器代码。最简单，最好的是使用程序集。大会是一堆非常奇怪的寻找命令，非常强大，快速地运行，因为它们几乎直接与计算机中的设备交易。汇编程序称为第二代语言。第一代是机器代码，或者有些人甚至将其称为硬布线。  
你似乎在谈论的是第三和第四代语言。过去30年来最常见的语言是第三代。它们都是基本上写在汇编程序中。程序集是汇编语言的编译器。不要让2个困惑。第三代语言看起来更像是英语第二代。这使得它们更容易学习和使用。我认为你的问题来自关于Java和C ++的笑话。 Java是用C ++写的，所以什么是C ++写在？大多数Java都写于C ++。一些基本基础命令仍然在装配中创建，以提供Java安全性，速度和稳定性。 Java仍然是可拍的。 C ++不太可拍，因为大多数是用汇编程序编写的。  
### 回答 102
tl;博士：你的问题就像询问哪种编程语言是全球网上写的。它没有用p写  
比特币不是用编程语言编写的，它用英语编写（带有痕量数学，代码示例和图纸和图表）。  
比特币协议的参考实现，称为比特币核心，它写在C ++中，但这不是比特币 - 它只是实现该协议的许多程序之一，并且几乎可以使用您可以的任何编程语言的实现想想（以及你从未想过的几个）。  
TL;博士：你的问题就像询问哪种编程语言是世界上写的网页。它不是用编程语言写的，因为它不是一个程序（它是一个协议，通信标准，通信标准）。  
P.S.忘记了链接 - 这是比特币（嗯，这个和所有比特币改进提案，A.K.A. BIPS，即扩展协议）：  
### 回答 103
你好世界 ，  
这是一个伟大而令人着迷的故事，它是如何开始的，并且在哪种语言中写入......  
起源 ：  
这一切都始于前端语言，实际上凯文Systrom在夜间学习到代码，并建立了一个名为Burbn的HTML原型（早期版本的Instagram）  
Kevin Systrom：  
Burbn枢转到更专注于照片 - 分享！  
在中期：  
凯文向他的同龄人展示，让他们使用并发现世界！  
并通过的日子，他遇到了党的投资者，并展示了他的Burbn，Hola！这是一个巨大的成功，他的Inve资助了500,000美元  
你好世界 ，  
这是一个伟大而令人着迷的故事，它是如何开始的，并且在哪种语言中写入......  
起源 ：  
这一切都始于前端语言，实际上凯文Systrom在夜间学习到代码，并建立了一个名为Burbn的HTML原型（早期版本的Instagram）  
Kevin Systrom：  
Burbn枢转到更专注于照片 - 分享！  
在中期：  
凯文向他的同龄人展示，让他们使用并发现世界！  
并通过的日子，他遇到了党的投资者，并展示了他的Burbn，Hola！这取得了巨大的成功，他的投资者资助了500,000美元，  
他回到了他的旧版Burbn，建立了一个真棒的杂乱无章的应用程序，他原样被拍摄了一张照片，并进一步改进它到达了成功的高度！  
流程图：  
使用的语言：  
具体而言，没有单个语言用于提高Insta :)  
一捆语言用于创建，其中一些语言是：  
后端部分（心脏）：  
操作系统 ：  
Instagram使用Ubuntu Linux 11.04作为他们的操作系统，  
各种前端技术，以方便用户  
等等。，  
谢谢阅读！  
编辑1;感谢您的支持，越过5k的观点，这使我的一天！  
### 回答 104
C是编译型语言 - 和编译器可以在所有任何语言编写。大多数C编译器是用C写的 - 但我敢打赌了好几个有一堆的C ++代码在他们。  
你可能不知道这是怎么可能的 - 在长期被遗忘的语言 - 但原来的C编译器，对于PDP-7 ...很第一个...是不是用C写的，我怀疑它可能是写在B（是的生成代码其中C是后继）。  
然而，不管它最初写的，很快就被重新用C写的后  
因此构建编译器，您使用的是它本身的旧版本编译的新版本（！）......而这正是我们一直在做，直到今天。  
因此，要建立（比如说）C编译器的Windows  - 他们可能使用的C编译器下的CP / M什么的Z80处理器。因此，步骤会是这样的：  
这似乎是不可能的 - 但它是什么是很经常做得到一个新的编译器和运行。  
其实，我与帕斯卡尔这样做。我设计了一个非常小的帕斯卡尔子集，它才刚刚成熟的足够写一个最基本的编译器，我写了编译器在BASIC子集 - 为Z80生成代码。我有很多的小帕斯卡尔子集的代码测试这个...当它已经足够好了 - 我在帕斯卡尔子集语言写了第二帕斯卡尔子集的编译器。现在，我可以说是写在BASIC编译器编译这个......现在我可以调试我的帕斯卡尔子集的编译器（Pascal写的子集）。当它工作不够好 - 我用它来编译本身并验证它生成的代码是相同的生成的BASIC版本的代码。  
然后，我扔掉了编译器的基本版本（这是一个丑陋的烂摊子！）  
现在，我可以逐步添加新的功能到我的子集语言的编译器，直到它能够收集到一个完整的Pascal程序。  
于是，我渐渐使用的语言，它现在可以编译的新功能改进编译器！  
在每个阶段，我会仔细检查新版本的输出是相同的旧版本...只是要确定我就没有断过一些重要的东西！  
最后，我完全Pascal编译器（用全帕斯卡）运作得非常好，我们都忘记了它的复杂的过去 - 它只是我们所使用的一切的编译器。  
### 回答 105
通常，当我们今天指导计算机时，我们以编程语言编写一个程序，这是一种正式的语言，对人类进行相对容易理解。然后将程序（或编译）转换为机器代码，或者实际的指令集实际计算机理解。程序员可能需要写入：  
<ol> if（年龄> 18）{do_something（）; } </ OL>  
<li>如果（年龄> 18）{</ li>  
<li> do_something（）; </ Li>  
<li>} </ li>  
但是计算机无法直接执行这些指令，他们需要翻译成一组指令，计算机可以理解。用英语写作，这些指令看起来像：  
<OL>加载值18 </ OL>  
<li>加载值18 </ li>  
通常，当我们今天指导计算机时，我们以编程语言编写一个程序，这是一种正式的语言，对人类进行相对容易理解。然后将程序（或编译）转换为机器代码，或者实际的指令集实际计算机理解。程序员可能需要写入：  
<ol> if（年龄> 18）{do_something（）; } </ OL>  
<li>如果（年龄> 18）{</ li>  
<li> do_something（）; </ Li>  
<li>} </ li>  
但是计算机无法直接执行这些指令，他们需要翻译成一组指令，计算机可以理解。用英语写作，这些指令看起来像：  
<OL>将值18加载到从寄存器B中的寄存器B减去寄存器B中的存储器中的值加载到寄存器B中的值，如果C现在是负</ OL>，则将C跳转到地址60的指令。  
<li>将值18加载到寄存器a </ li>中  
<li>将存储器单元号50中的值加载到寄存器b </ li>中  
<Li>从寄存器B中减去寄存器A，存储C </ Li>的结果  
<li>如果c现在是负</ li>，则跳转到地址60的指令  
这是一个更难理解的。要使它更加困难，计算机不会以英语为单位。instead，计算机能够执行的每个基本指令都有一个分配给它的数字，并且您使用此号码来告诉将计算机执行。如果加载到A是指令编号02，从存储器加载到b是指令37，从b中减去a是指令59，如果c是否定的，则跳转为指令42，那么计算机将看实际指令类似的东西:(我在这里使用的说明性计算机采用指令和参数作为2位基本-10号码，实际计算机使用二进制文件，但这对IT原理没有差异）  
<ol> 02 18 37 50 59 42 60 </ OL>  
<li> 02 18 </ li>  
<li> 37 50 </ li>  
<li> 59 </ li>  
<li> 42 60 </ li>  
我在这里拆分指示4行，让更容易看到程序是相同的，但实际的计算机代码没有任何类型的格式化，计算机程序实际上只是：  
<OL> 02183750594260 </ OL>  
<li> 02183750594260 </ li>  
那么如果你没有任何程序翻译我在这个答案中写下的第一个程序，那么在这里唯一的语言唯一的语言就是实际的计算机硬件将理解的唯一语言？你手动做到这一点吗？你手动做翻译，你自己，使用笔和纸，也许是一个计算器。写一个整个编译器或翻译，以便是现代的高级语言，这种方式将永远采取，所以你所做的就是你在几个非常小的步骤中做到了。一个是写的一个非常基本的汇编程序。它可能会作为输入的输入：  
<OL> Stora 18 LoadB 50 Subab JMPNEG 60 </ OL>  
<Li> Stora 18 </ Li>  
<li> loadb 50 </ li>  
<li> subab </ li>  
<li> jmpneg 60 </ li>  
并将其转换为机器代码，通过使用02替换字符串Stora的相对简单的机制以及带有37等的字符串Loadb的机制。您现在已经从裸机上迈出了一步。您现在可以稍微编写程序更有人类友好的语言，如斯库拉18而不是0218.we致电这样的翻译器。您可能希望在汇编程序中编写一个更好的汇编程序，并使用您手工编码的基本汇编程序将其组装到机器代码中。写一些类似程序，该程序将C代码转换为装配代码，并达到可以在C中进行编程的级别。  
如果所有这一切都使你的头部旋转，将其视为粗糙的视图：如果您需要工具制作工具，那么第一个工具如何构建？如果要从第一个原则制作电锯，但您注意到了您需要大量的工具来制作该工具，您如何开始呢？答案与计算机 - 程序的答案相同。您从制作一个如此简单的工具，您可以设法使用这么简单您已经拥有的工具，即您自己的两只手和自己的大脑。您可能首先将两个合适的岩石一起敲打，以产生锋利的边缘，然后使用葡萄藤或其他东西将锋利的石头绑在树枝上并拥有一个基本的斧头。 。  
### 回答 106
以太语编程是什么语言？  
嗨拉瓦尔，  
谢谢你的问题。  
Ethereum主要以C ++，Python，Golang和稳定性编程。  
查看此处的github存储库以查看代码......以外  
### 回答 107
大多数加密电脑软件（钱包，矿工）是用C的C ++编写的，更小的百分比。除了在C ++中的代码之外，我认为Ethereum还有Python API。  
### 回答 108
 决定语义：您将n  
创建一种编程语言是一个漫长的过程。  
分析域：每种编程语言是为了解决一组特定的问题。因此，作为第一步，你要什么问题你的编程语言是要解决决定。例如R编程语言是专门设计来解决在统计计算和数据分析，数据挖掘和图形领域，而C，锈等可称为通用语言的问题。你需要找到的东西应该你的程序是能做什么，以及如何很好地实现它。  
 决定语义：您需要在您的编程语言的结构，如什么是将要在INT数据类型决定的，它是一个面向对象或程序语言，应该有语言中的一些基本功能或将它需要的库，与其他语言如C等接口  
建设规划：您将需要考虑可能的用途，人们又可能如何使用您的编程语言和做什么。你可以了解如何使最用户友好不影响灵活性。  
语法规划：书面语言和试验它的计划的语法和结构。看看如果你喜欢键入代码中所说的句法。应当注意，使语法应该是容易的，但有效的为您计划使用。您应该使用正则表达式系统来测试您计划的语法只是为了确保一切声音。如果你不知道正则表达式你必须大概是从学习它们是什么以及如何使用它们启动  
翻译/编译：您必须仔细决定，如果你的语言应该是一种解释型语言或基于上面提到的所有其他因素编译语言。这可能对语言的实用性巨大影响。  
写译者：然后您需要编写语言翻译或以其他方式被称为编译器或解释你的程序，这本身就是一个很大的话题。 >阿尔弗雷德V.阿霍，莫妮卡·S·拉姆，拉维·塞特和杰弗里·乌尔曼 - 有关编写编译器和解释器的最佳指南，你应该从龙书阿霍，林，乌尔曼和塞提的新eddition启动  
### 回答 109
印度教的智者会相信他会在Devanagari Lipi这样做。名称本身大致转化为上帝写作系统。  
其他宗教会相信他用与他们崇拜的众神起源相关的语言代码。  
我说出语言，而不是编程语言，因为上帝不需要一个人为代码的语言。如果他想计划，他应该能够以他喜欢的任何语言造成的程序。  
上帝是上帝，永远不会公开出来，说出他更喜欢的语言。会有一些语言得到很多支持，他们将被神的恩典声称它，所以上帝必须支持他们的首选语言。  
雅典主义者会在他所做的一切中看到美，所以他们会声称他用蟒蛇（红宝石师者会不同意的东西）的代码  
无神论者只是不相信上帝，所以他们不会支持整个论点。  
不可知论者将坚持认为，如果上帝计划或他更喜欢的语言，并且采取中性立场，则不能肯定地知道它。  
大公司将举行无数的会议，并组织10小时长的研讨会，通过PowerPoint幻灯片遍布数百次幻灯片，引用了上帝更喜欢Java或.NET只是为了安抚数千名员工并让他们保持动力。  
极端主义者抚摸所有改变并声称上帝可能在COBOL中的计划。并坚持认为，在他的每一段代码中都有一个评论文本，指示我们灭绝支持其他节目语言的每个人。追随者将永远将其代码编译为零错误一旦到达天堂。  
### 回答 110
网络软件的基础设施（例如套接字库和  
Java，PHP，Perl，C ++，Python，JavaScript和许多其他人（我记不用我不使用他们的平台的Microsoft Web语言）。  
互联网是一个独立网站的网络，所有这些都以他们的所有者选择使用的任何语言编写。我列出的语言 - 以及任何语言的大多数Microsoft用户更喜欢 - 都是用于编写网站的最常用语言之一。 Internet上还有其他类型的网站和客户端（例如，VoIP服务器和客户端，命名一个示例）。  
网络软件的基础基础设施（例如套接字库和实现各种互联网协议的所有网络守护程序）是用各种系统编程语言编写的，通常是C或C ++。  
互联网不是统一的实体。它不是用一种语言写的。它是一种广泛，宽松的服务器和服务联盟，而不是单一的集成产品。  
### 回答 111
你可以使用谷歌搜索已经容易回答你自己这个问题。  
根据维基百科：  
在20世纪40年代，创建了第一个可识别的现代电功率电脑。有限的速度和内存容量强迫程序员调整汇编语言程序写手。人们最终意识到汇编语言编程所需的智力劳动很大。  
旨在指令传送到计算机的第一个编程语言写于20世纪50年代。早期的高级语言来设计的计算机是Plankalkül，由德国人1943年Z1和1945年。然而，它没有实现之间开发的康拉德楚泽，直到1998年和2000年。  
约翰Mauchlys短代码，在1949年提出的，是对电子计算机有史以来第一个高层次的语言之一。不像机器代码，短码声明中表示理解的形式数学表达式。然而，该方案有充分的它运行时被翻译成机器代码，制作过程不是运行相当的机器代码慢得多。  
在曼彻斯特大学，阿利克·格伦尼开发自动编码在50年代初，与马克1由R. A.布鲁克于1954年开发的第二次迭代，被称为马克1自动编码。  
