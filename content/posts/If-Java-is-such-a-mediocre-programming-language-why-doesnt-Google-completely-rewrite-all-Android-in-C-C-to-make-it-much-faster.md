---

title: 如果Java是这样一种平庸的编程语言，为什么谷歌完全重写了C / C ++中的所有Android，使其更快？
date: 2022-01-23T22:08:09+08:00

---




## 如果Java是这样一种平庸的编程语言，为什么谷歌完全重写了C / C ++中的所有Android，使其更快？  
### 回答 1
这是一个很好的拇指，用于表单的所有问题，如果<premise>那么为什么不只是<afticle>：答案是您的前提是错误的。  
JVM比Native C ++慢得多（我认为1.5×是典型的），但这种情况下的性能可以在便携性上提高了一个主要的提升。随着vladislav zorov已经提到，Android在包括土豆的所有内容上运行。为每个架构编写应用程序将超出大多数开发人员的功能。 JVM¹从中隔离您。您只使用一个虚拟架构，将其映射到实际的底层硬件的痛苦留给JVM¹Devs来处理。  
此外，对于您使用的95％的应用程序，性能根本没有意义。例如，Quora²应用程序非常完全I / O绑定。它将在C ++中写入，服务器响应不会越快地到您的手机，也不会更快地打字。  
¹ -  jesse pollard正确指出，Android技术上不使用JVM，它运行在Android运行时（ART），在某些重要方面存在不同，最后，它将Java字节码到Native ELF可执行文件中。  
² - 遵守一些评论者，谁用Quora发出问题是一个特别非常令人难以置信的写作的应用程序（虽然它完全无关紧要），这里有二十多个应用程序，也不会在最轻微的中受益，以便在C ++中重写。他们的表现受I / O的约束：  
Instagram，Facebook，WhatsApp，Messengergmail，Google搜索，谷歌翻译，Google Docstwitter，WebToon，9Gag，亚马逊Shoppingduolingo，Audible，Booking.com，Viberplugshare，Waze，Google地图，Sygic导航  
答案绝对没有，但我希望你们都快乐。  
### 回答 2
很少有误区，我会解决它们：  
1.注意所有Android操作系统都是用Java编写的  
您还记得Windows Phone是否有很多在C＃中写入的组件和应用程序，比您今天可以找到的最便宜的Android，它是一个不可行的电话操作系统？有人会说： DirectX，Windows NT内核和许多组件都将写入C中，因此只需要一层薄层来快速制作应用程序。相似，在Android中的C ++（或C）编写了大量逻辑，也许它们是作为性能的大多数关键零件是关注：驱动程序（如视频驱动程序），许多库（Skia绘图库也在C ++中写入），依此类推，假设80％的应用程序大多数显示简单数据，即使Java将显示简单数据在Android上缓慢，它不会影响最终的用户性能  
今天最多的应用程序不是表现束缚  
这可能听起来很傻，但是Android用32位约0.5 GHz（或500 MHz）的手机发射，一切都慢（包括java实施，这是一个翻译），它们是可用的手机。我不能说我会喜欢现在与他们一起工作，但在Java的时候，java正在运行，只有在解释器下运行，所有工作的事情（如地图，电话，议程，日历，天气）完美无瑕.3.java没有解释，如果不是很快，那就很快就会很快与今天的C ++相比，我们拥有完全编译的Java（或Dalvik）字节码到本机实现，归功于ART.在Dalvik的字节码内完成了在您的设备中删除之前完成的程序优化。此级别（未在C ++编译器上设置命名的LTO  - 链路时间优化，除了您特别做。这意味着在背后发生一些虚拟化和内联，无法使用C ++代码的框中出现。这是尤其是虚拟CAL l /调度往往避免。如果您在c ++中拥有非常oop应用程序，您必须使用相当多的技巧来实现相同的优化  
4.Java是比C ++更流行的语言，也是一个更简单和更安全的语言  
这个因素也是关键的：您可以使用java为您的Android应用程序以及您的后端服务在线.Imagine用C ++执行此操作。Java不仅仅是处理一个按钮后面处理逻辑的语言，但很可能您将希望从Apis.imagine获取一些外部数据也是现实生活：您有一个崩溃。你知道，默认情况下，C ++没有堆栈跟踪的信息在异常中？因为Java做了它。它有帮助很多，你不这么认为吗？或者你只写异常的免费代码？如果你这样做，你如何让所有软件都有异常？5.java比c ++慢得多，但有时是（很多）fastereven java仇恨通常同意Java是快速的，如果不是最快的语言，则可以代码某种类型的应用程序.LEALLLY，尝试一个非琐碎的基准：如果它有一个序列，请尝试检查一些大DNA数据集，并将其与C ++实现进行比较。您可能会看到Java可以赢得胜利。有些人测试真实生活DNA序列处理，发现Java是最快的，但使用大多数内存。我确实测试了自己的Java声明，我发现Java未优化，并且代码至少可以更快地制作2倍，并且使用少于5倍的内存。所以，我提取了图片以简化查看：Java可以在少于7分钟的大型数据集中处理：  
而且我对Java算法的改进确实带来了以下数字：  
请注意，我使用了一个较小的数据集，但原理仍然存在：Java可以从2分钟（原始，意味着在第一图中的中间列中的原始列）优化到少于半分钟。但是Java更快在所有其他指标中：开发速度更快，找到一个错误（如内存泄漏）更快，重建更快，更快，执行一些企业准备模式的代码等依赖性注入速度更快.6.java是一个平台，当C ++没有，你在C ++中访问一个文件吗？使用<fStream>可能。在下载的东西有什么样的东西？你是否使用卷曲？您如何为当今的手机和所有后代部署如何？你会静态链接卷曲吗？为什么不更好地使用拥有所有内容的Java。如果要启动一个简单的Web服务器，则为什么？  
### 回答 3
问题是应用程序，而不是那么多的Android本身（这肯定是由C ++编写的，我怀疑他们使用Java进行任何性能关键部分）。由于Android设备附带各种CPU，因此用C或C ++的语言编写应用程序意味着每个开发人员都需要单独编译Android运行的可能CPU架构的版本，并确保其代码没有未定义行为，是便携式和东西，这是很多工作。  
也就是说，我认为您可以在C ++中编写模块，并通过JNI将它们链接到Android应用程序中。但是，你有上面列出的所有问题。  
相比之下，iOS几乎是微不足道的，它只在iPhone上运行，只有Apple CPU。这可能是为什么一切都在那里跑得好 - 没有Java，没有口译员，没有JIT编译器，只是纯粹的本机代码。  
### 回答 4
更快的是计算机程序员的阴茎长度。它重要的方式，比他们认为的方式少。但他们肯定永远不会厌倦谈论它。  
Java VS C / C ++的优点几乎具有性能ZILCH。首先，两者之间的差异通常很小。其次，即使在大多数域中，他们也无所谓。 1毫秒VS的差异为2毫秒响应时间对用户无形。  
有特定的域名，这种表现很重要 - 但大多数Android都不是它。也许是内核或驱动程序，或UI的一些响应部分。对于绝大多数应用和特征，性能差异可以忽略不计，无关紧要。  
程序员yammer关于性能，因为这是他们唯一知道如何衡量的东西。他们不知道如何衡量易于升级和在团队中开发的能力或零日黑客的能力。这意味着他们不知道关于让软件进入用户手势并使他们快乐的事情。  
### 回答 5
好吧，让我们从前提的唯一部分开始是正确的。  
Java是C ++，带有无法学习C ++的人的训练轮。  
几十年来，IT行业一直是Dumbing-Down软件开发。当前技能阈值的底部是Python。  
以下是如何判断这些事情：  
您是否必须在软件开发或其他一些部分获得使用它来获得工作的大学学位？如果没有，请向底部打招呼。  
谷歌想要：  
1.他们的手机操作系统可以访问机器人......  
### 回答 6
Android的重要部分已在C ++中写入。许多包括我自己的开发人员在其Android应用程序中使用本机代码。在C ++中重写整个操作系统不会使应用程序运行得更快。应用程序可能会慢，使用大量的CPU和电池，因为编程技术不好，而不是因为编程语言或操作系统。  
将所有内容迁移到C ++会影响便携性。源代码大小将增加，递增维护成本。很多Java和Kotlin开发人员会变得不快乐。至少最初，崩溃和漏洞的风险将增加。  
高通公司有一个名为BREW的开发平台。该应用程序是在C ++中开发的。应用程序提供非常有限，因为很少有公司希望支持本地平台。 J2ME比啤酒更受欢迎，因为它是Java。一些J2ME平台编译了Java到原生代码，而不是字节码。所以，关键是Java绝对不是平庸的。  
### 回答 7
奇怪的是，我没有找到答案，这是最适合这个问题的答案。  
编程语言可以以不同的方式编译和执行，不同的系统等。  
在主系统上，Java在JVM上运行。哪个（相对）慢，由于该事实，即代码必须解释，它不是本机运行环境。  
理论上，我们也是可能的，我们将Java直接符合到原生。如果是这样，则代码将在一台计算机上运行更快，而不是其他人。  
谷歌怎么样？  
运行Android的所有手机都在兼容的CPU-S上运行（在32到64位之间存在间隙  
奇怪的是，我没有找到答案，这是最适合这个问题的答案。  
编程语言可以以不同的方式编译和执行，不同的系统等。  
在主系统上，Java在JVM上运行。哪个（相对）慢，由于该事实，即代码必须解释，它不是本机运行环境。  
理论上，我们也是可能的，我们将Java直接符合到原生。如果是这样，则代码将在一台计算机上运行更快，而不是其他人。  
谷歌怎么样？  
运行Android的所有手机都在兼容的CPU-S上运行（32到64位ARM-S之间存在间隙，因此使用本机代码是完美的。这就是谷歌的所作所为。它将JVM代码（不是J​​ava源）编译为本机代码。此编译器中有大量的优化，因此以Java编写的代码在例如在C ++中写入的速度几乎相同。  
此过程复杂，具有更多步骤，但您可以阅读更多文章（Android运行时）。  
### 回答 8
虽然Java离开了一些需要的东西，但它与编程语言并不糟糕。然而，C陈旧，涉及陷阱，C ++在该片状基础上建立。  
速度不是唯一的良好语言的衡量标准，实际上它是一个次要的措施，更依赖于编译器的实现技术，代码生成器广告优化器。  
C和C ++通过避免在安全的多程序环境中必不可少的界限检查来获得它们的一些速度。程序员喜欢速度，因为它很容易衡量，而支持编程过程的其他品质更难以衡量并受到很多人的意见 - 大多数误导。  
您是否在考虑在语义上基于C / C ++语言？这是一种设计语言，操作语义的差。  
我们实际上可以比这三种语言更好地做得好，但现在已经过去的时间和C ++应该退休，并且以这些遗留语言编写的软件越来越少。 java的坏点主要基于它来自c ++的结果和共享。  
Android基于Linux（最终用户的不安全系统），您不会在基于Linus Torvald对C ++的众所周知的众所周知的C ++中完成的任何内容或大部分Linux（也由Ken Thompson等其他C人分享） 。  
如果飞机行业像编程行业一样缓慢地移动，飞行员仍将在20世纪70年代而不是波音747s飞行20世纪20年代的双层。 C现在正在积累到编程。  
我看到罗兰休斯在Quora的工资墙后面有一个答案。这并没有让他更可信。我可以看到的一句话是'Java只是C ++带有训练轮'。这个“训练轮”的概念和歌词是一种废话，长期以来，C社区不了解软件开发的先进原则和技巧。  
其他语言具有以问题为导向的编程支持，该语义可以检查，而不是缺乏语义的机器导向的编程。  
“初学者的训练轮”短语只是休斯和他的ilk喜欢扔掉的懒惰废话。这不是它不是诱人的，因为人们不喜欢将自己视为初学者。然而，它是相反的 - 当人们不了解语义节目时，他们真的只是初学者。  
然后休斯说，几十年来，IT产业一直在挫败编程。在这种情况下，这是真的 - 它们继续基于C的基础语言而没有真正解决问题。这是使编程较少机器导向和更具责任的导向。休斯描绘了像愚蠢的一样，但它正在聪明。  
### 回答 9
完全重写其他语言的严重系统是一个非常费力的业务，它可以实际上需要数月甚至几年。语言本身并不缓慢或快速，这是潜在的基础设施和次优算法效率 - 这与语言没有任何关系 - 这可能会在运行时慢下来。在编写一些新的东西的时间内，语言可以快速或缓慢，Java非常详细，它需要比说Python更多的打字，您可以在那里可以获得更快的东西并运行更快。另一方面，C / C ++，是的，理论上跑得更快，但你会花费尽可能多的时间 - 如果不再 - 写作代码。  
并且严重，你不能称之为今天最受欢迎的语言，你看到，它不仅仅是语言，它是生态系统，所有的库，框架和底层基础设施，这就是让事情变得更加容易最终更快。  
### 回答 10
问题Android解决了支持的各种硬件，而无需HA  
问题不是Android操作系统，问题是编写了不良的Android应用程序。操作系统本身就足够了，但有些（很多？）Android应用程序是热的垃圾。就像我的第一个iPad空气上的iOS就足够了，但是Quora Mobile Client是一个绝对的Shitshow（认真，是每一个击键都必须击中服务器或其他东西？这就像在10个波特终端键入。我可以在第一个字母渲染之前写入整个段落 - 在iPad虚拟键盘上 - 在第一个字母渲染之前）。  
Android解决问题正在支持各种硬件，而无需为每个平台调整代码，您将绝对与C或C ++执行。由于Android是基于Java的，您可以编写代码来定位一个平台（JVM），而不是担心任何特定于平台的怪异。  
JVM处理您的所有平台特定的怪异;您可以编写一次代码，它将到处运行。这值得任何对本机代码的绩效损失。  
### 回答 11
它缺点速度和易于编程的速度，但更多的是使其兼容的情况，例如，如果我发现一个Flappy鸟类，我可能会安静地在像素6as上轻松地运行它，就像它被释放时的设备一样。更不用说Android被建立了GNU + Linux，它本身就是很多基于Java的基于Java的应用程序完美无瑕地运行（例如我的伙伴旧笔记本电脑无法运行vanilla Minecraft 1.7.10但是将Linux Mint放在它上，可以运行版本，最小的诊断）  
### 回答 12
对于大多数应用程序来说并不慢，它可能比C ++代码更安全。  
谷歌显然正在编写新的操作系统，不确定他们使用的平台，根据维基百科，语言是C，C ++，Dart，Go，Rust，Python  
此外，请注意，Android的部分基于Linux内核，因此在C中实现，应用程序在Java中实现  
### 回答 13
当C＃首次开发和发布时，我在Windows上工作。围绕C＃VS C ++编写代码的性能和可靠性影响有很多讨论。  
首先，让我们假设你的意思是非托管的C ++与托管C＃，因为（或至少用于）C ++的托管版本。  
我从未为C＃VS. C ++的影响找到普遍，可靠的评估。表现确实受到影响。对于对我来说最重要的事情 - 科学计算，二进制包装和解压缩数据，以及阵列操作 -  C＃的开销在10％和15％之间超过C ++。请注意，这是2000年初。  
根据您如何编写代码，性能影响可能会高得多，例如因为效率低，频繁的拳击和变量释放。更糟糕的是，由于垃圾收集，性能可能会爆发。  
已经说完了这一切，除非你正在进行一个非常低的功率设备，我建议在C＃而不是C ++中开发。我的经验是，C＃代码比等效的C ++功能更快的速度大约40％。换句话说，我可以在C＃或5个月内在3个月内写一些东西。  
你懂得越多…  
### 回答 14
问：为什么C速度比C ++更快？A：不是。 C ++更快。  
非正式地，每个C程序都是C ++程序，并且Visual C ++，GCC和CLANG中的编译器后端是相同的，因此它很可能是真正等效的程序生成相同的代码，因此以相同的速度运行。  
这意味着每个C-oder-over-c ++基准使用不同的程序，通常将高调的C代码与天真C ++代码进行比较。当C ++也有阵列时，它们将C阵列与C ++向量进行比较。它们比较C堆栈上分配的C alloca（），以c ++新增，它在堆上分配。他们使用C ++分配行为的特殊知识来编写一个基准，其中C跑得更快，比如John Harrop在此线程中执行，当使用相同知识的C ++程序员可以通过限制动态的范围来加速其代码到等效级别。分配变量如下片段所示（注意每个分配周围的额外卷曲括号）。  
<ol> for（int i = 0; i <1000; i ++）{{vector <int> a（1000000）; x + = a [0]; } {vector <int> b（1000000）; x + = b [0]; } ... </ OL>  
<li> for（int i = 0; i <1000; i ++）</ li>  
<li> {</ li>  
<li> {vector <int> a（1000000）; x + = a [0]; </ li>  
<li> {vector <int> b（1000000）; x + = b [0]; </ li>  
<li> ... </ li>  
C restrict关键字是一个异常，在C ++中不可用，这可能会使一些C程序运行得更快。  
因此，剩下的是C ++功能不存在于C ++程序更快。包括内联函数和模板的功能，使中转化优化更加丰硕，更快的算法（std :: sortus与qsort（）），c ++长度+缓冲区字符串与c null终止字符串，c ++异常处理与铲斗之行时尚的返回错误代码相比，毫无疑问，许多人比我不能立即打电话。  
然后我们拥有C ++功能，使得更容易编写最佳代码，如虚拟功能与IF / else if chains或switch语句。使用函数指针，可以从C获取C ++的性能，但仅具有特殊知识。 （我承认，当您不需要它们时，也可以使用虚拟功能，并将C ++程序减慢。）  
回答这个问题的大多数人（包括我到目前为止）不提供证据支持他们的索赔。我钦佩约翰哈罗夫这样做，即使我争取了他的结果。  
### 回答 15
近2年后最初提出这个问题，答案仍然是一样的：假设它写得很好，编译完全，并在标准的x86或x64 cpu上运行，而不是汇编语言以外的流行语言即将更快地执行然而，C.然而，有大量的其他语言可以更快地开发代码在C.这一切都取决于您的目标;快速运行代码或快速开发代码。  
### 回答 16
C ++不会成为初学者程序员的噩梦。  
C ++可以成为初学者程序员的噩梦。如果它确实如此，它是总是教学。  
有人说，它是语言（最终后跟一个真实方式的示例，方案编程语言必须遵循，而C ++没有）。这是一个不成熟的观点，不值得考虑。  
C ++的问题，同时其主要优势，是它给你的自由。每当语言可能为你做出选择时，它就没有了。虚拟与非虚拟？两个都。多重继承？在那里;你不必使用它。不，没有超级对象一切都必须衍生自;您可以构建您自己的类层次结构，最好是当它有意义。是的，这是一个完全装载的枪，你可以用来射击自己;请不要。  
与此同时，C ++带有兼容原因的东西的行李。即使在一开始，它在被称为C ++之前，语言必须（大多数）与C兼容，它仍然是。那是因为C ++不是教学的语言，而是一个用于解决现实世界问题的语言。打破现有代码 - 写第一玩具程序时不是一个问题 - 是一个大的禁忌。  
OTOH我们必须明白，如果您不需要其最先进的功能，则必须更容易使用 - 例如，即没有初学者会写通用库，对吗？ - 并允许语言帮助您。  
C ++比似乎更容易。它更容易初学者，通常更容易地教授指针和内存（完全没有意识到您的应用程序运行的硬件），这比IMO少于理想），并且也更容易满足某些资源和性能如果你有必要的限制。  
那么，可以让C ++成为一个噩梦？它简单地分解了这种复杂性。我将列出下面的几个（imo）糟糕的想法。  
首先教这只对毫无戒心的初学者来说只使事情更复杂，使他处理指针和内存管理以及在开始时不必要的其他东西。这可能会令人沮丧，穷人的初学者将在使用更简单的语言时跳跃。  
与此同时，他是在C ++中没有地方的学习技术;当然，他也会试图在那里使用它们。这不仅更困难，而且对一个人的学习也是有害的！  
不使用C ++的语言功能。说，第二个程序一个人会写的是一个显示你好的人<name>！这与STD :: Strings非常容易;您不需要知道字符数组终止为零，关于指针，关于内存分配，您甚至不需要考虑保留静态缓冲区以及它应该具有的大小。你只是有两个字符串并连接它们。  
但在某些时候，您可能会尝试实现自己的，有限（也许是非通用）字符串，只是看它是如何完成的。你可以做到这一点。  
没有从好来源学习。 Rob Nelson提到了一些宣称所有用户定义类型应该是通用的教程;那里有很多糟糕的学习来源，应该彻底避免。  
这可能导致：  
实际上，您可能会推动更容易的语言，或者更糟糕的方式写入坏程序。  
IMO Bjarne Stroustrup的编程：使用C ++的原则和练习定义了一个好的，逻辑和简单的学习进展。  
我还推荐Kate Gregory的停止教学介绍CPPCON 2015，以及Bjarne Stroustrup从CPPCON 2017的学习和教授现代C ++。并且在这个主题上有很多更有趣的材料。  
### 回答 17
最短和最直接的答案是你的问题，这取决于你的目标。  
但我建议你先学习JavaScript，因为： -   
知道Java的大多数程序员是因为他们遇到了他们在学校和学院介绍的计算机科学课程中的第一语言。现在大多数大学正在向Python等语言转移。对于桌面应用程序开发Java正在失去其优势。但Java仍然是一种行业标准语言。  
如果您想开发Android应用程序，其中需要Java，您可以使用JavaScript开发您的应用程序作为Web应用程序，然后使用PhoneGap等内容将其转换为智能手机应用程序，不仅适用于Android，还可以为Windows和iOS转换为智能手机应用程序。  
如果你从工作位置看，  
Java是行业标准语言大多数公司使用它的应用程序开发，网上有很多问题询问为什么不从Java转移，你将读取答案，就像他们已经拥有Java中的一个巨大的CodeBase一样花点时间将整个代码库转移到其他一些技术，java很容易维护和更新代码，很容易找到良好的Java开发人员等。  
如果您查看Web开发方面，javascript也是如此。  
我想传达的是Java是可更换的，随着时间的推移和其他技术的起源，慢慢替换。  
但JavaScript是不可替代的，您可能会发现像Brython for Python等的其他框架，但无可以在性能和浏览器兼容中击败JavaScript。  
直到您愿意根据这些语言学习某些框架，您是否选择JavaScript或Java它并不重要。对于任何好的项目，您需要抽象，即第三方图书馆和框架来构建新的东西。不打算只是为了学习基本的语法，变量，类型，OOP等，但在学习所有这些之后，可以获得一些好框架。  
即使您只是希望了解像数组等语言的基础知识，我建议JavaScript，因为它具有更清晰/用户友好的语法。  
使用JavaScript，您将了解某些名为事件驱动编程的内容，您的代码片段将基于网页上触发的事件运行，例如，按钮单击网页可以同时启动功能，同时悬停它可能会发射一些另一个功能。在您正在进行GUI开发之前，您将无法在其他编程语言中进行此操作。  
P.S.  
### 回答 18
首先，我想我应该使C ++和Python是语言的强制性点，而速度不是由语言定义的。如果足够的正确人员想要足够糟糕，他们可以编写一个比某些方式慢的C ++的实现通过一些任意数量的Python的具体实现。答案的数量归因于速度的差异，以便在Python被解释的情况下编译C ++的事实。这只是at best.cpython（我很确定使用最广泛的一半） Python的实现）有点像Java，.NET等。这是，它通过将源代码编译为虚拟机的对象代码（通常存储到.pyc文件中）开始。该代码是由在模拟器上运行它（如果您更愿意以这种方式查看该虚拟机。要汇总，它通常不会直接从源代码中解释。事实上，实施方式非常类似于Java通常是在ne的时候回来了w.java以来​​，通过使用JIT编译器来加速了很多。当您运行Java程序时，JVM将解释/模拟代码一段时间，但如果代码经常运行，则JVM将使用拍摄JVM字节代码的第二个编译器，并为其执行的机器生产本机的本机代码。这可以（和确实）加速执行相当一点 - 虽然Java仍然略微慢于合理写入的C或C ++，差异的余量很小（例如，30-40％较慢相当普通，并且在某些情况下，边际甚至比该余量小）。没有特别好的原因，你不能用Python做出大致相同的事情代码拍摄.pyc文件，并将其编译为其运行的特定计算机的本机代码。虽然cpython不这样做，pypy do.in事实，这几乎所有的小标题都是：python的JIT编译器效果很好。很常见的例程只需通过运行更快地运行7-8倍在小卵体中而不是cpython（尽管确切的改善当然也有所不同）。即使使用了pypy，Python代码也很少用C ++保持。在某种程度上，这是通过使用JIT编译器解释的（可能）上面解释的，Java通常比C ++慢。与Pypy的快门仍然比Java与Java仍然比JiT.Part的速度慢，这无疑是一个不能承受的较小的项目，这是一个不得不承受的时间和努力JVM已经GOTTEN.I不认为这解释了整个差异。与pypyeveneven仍然常规运行比Java速度慢3倍左右。我认为大多数差异来自Python使用动态打字的差异源头，虽然C ++和Java都使用静态键入。这意味着在Java或C ++中，表达式的类型在编译时是已知的，因此编译器可以直接发出对该特定类型的请求的操作的机器代码。（例如，如果您有类似类似的东西，如果a和b是int类型，处理器最终通过单个添加指令进行添加）.by对比度，python（非常着名）使用鸭键入。如果我有一个像+ b在python等+ b的表达式，我就可以了通过代码跟踪并证明两个A和B都包含int，因此我可以编译到单个添加指令的添加，就像我将在c ++或java中那样。但是它取决于编译器，以证明它可以，没有程序员的帮助。我不完全肯定，但如果帕契可以做那些至少部分时间的那种追踪，我不会感到惊讶，并且会尽可能发出静态键入的代码。然而，在很多情况下，都会发出静态键入的代码。然而，提前知道特定变量是否将包含，例如整数或字符串是困难或不可能。因为这，Python代码通常必须检查它在执行操作之前处理的对象类型的对象。因此，在做加法之前，它必须弄清楚A类型是什么，而且t键入B是，申请类型的操作是什么操作。这是Python中的慢速，这更难以避免 - 而且相同的情况也适用于几乎任何其他动态类型的语言。  
### 回答 19
我们都可以辩论为什么Java被选为Android编程语言，但它将是我们的所有解释。这种情况最终与安迪鲁宾休息，他是最好的回答这个问题的人，他实际上回答了这个问题。  
阅读本文。  
试用：Android首席在为什么java为Android挑选ZDNet.  
我将再现一些他的回答的一些亮点。让我们从马的嘴里听到它。  
当Google Catel robert van巢提示时，如果还有其他可以为Android工作的编程语言，鲁宾肯定有可能。其他一些l  
我们都可以辩论为什么Java被选为Android编程语言，但它将是我们的所有解释。这种情况最终与安迪鲁宾休息，他是最好的回答这个问题的人，他实际上回答了这个问题。  
阅读本文。  
试用：Android首席在为什么java为Android挑选ZDNet.  
我将再现一些他的回答的一些亮点。让我们从马的嘴里听到它。  
当Google Catel robert van巢提示时，如果还有其他可以为Android工作的编程语言，鲁宾肯定有可能。为Android考虑的其他语言是JavaScript，Python和Lua。  
使用Java进行智能手机的好处 - 主要是着名的品牌名称以及兼容性，即它是全球大学教授的共同语言。  
我们认为这是开拓Java的机会，我们要求太阳为开源社区做出贡献，鲁宾说。  
这篇文章有很多关于如何以及为什么Andy Rubin与Sun（Oracle收购太阳前的Ady Rubin）以及为什么谷歌想要为Java进行不同的许可，以便可以接受Android开发。这是一个非常有趣的文章，我建议阅读它。  
基于这一切，我可以说是 -  java被选中，因为它是流行的，广泛的编程语言。 2005年，当Android在它的新生阶段时，Java程序员可能是更容易和最大的人才池。  
我想知道为什么开发人员社区会发现它有趣的是学习一个不太常用或完全新的编程语言（如果Android实际上已经提出一个），那么即将到来的未知移动平台。  
我相信有很多技术原因（其中一些其他答案已经尝试指出）为Android开发选择Java，而是其中一个主要原因是，他们（Android Inc）希望通过程序员社区快速采用帮助Android快速增长。  
在后古，我可以说它为他们工作了。  
编辑1：  
看起来这是非常常见的问题。这就是为什么我在这个主题上创建了关于我的YouTube频道编号的更详细的视频。  
### 回答 20
让我们移动  
Java和C ++两种编程语言都有自己的优点和缺点，就像任何其他编程语言一样。  
让我们首先了解Java  
让我们在C ++语言上移动  
C之后，C ++带来了一些很好的新功能。它使编程语言更容易更容易进行程序员。  
如果您想要更多与程序员相关的答案。你可以遵循ashish pandey（आशीषपांडे）。  
谢谢。  
### 回答 21
这不是一个有利的原因列表  
### 回答 22
对于游戏？ C ++（您无法找到许多在C ++上寻找Java开发人员的游戏公司）。  
其他一切？ Java。  
大多数公司基本上需要一个美味的业务规则引擎，可以轻松地在多个不同的机器上运行，因此它会粘在Java上。  
Java具有比C ++更简单的语法。  
Java有一个更好的通用数据库API（JDBC）。  
分发Java库比C ++要容易得多。 C ++没有真正具有有效的Maven / Gradle类型的分销系统的原因（这里看着你，柯南https://conan.io/）。大部分的第三个p  
对于游戏？ C ++（您无法找到许多在C ++上寻找Java开发人员的游戏公司）。  
其他一切？ Java。  
大多数公司基本上需要一个美味的业务规则引擎，可以轻松地在多个不同的机器上运行，因此它会粘在Java上。  
Java具有比C ++更简单的语法。  
Java有一个更好的通用数据库API（JDBC）。  
分发Java库比C ++要容易得多。 C ++没有真正具有有效的Maven / Gradle类型的分销系统的原因（这里看着你，柯南https://conan.io/）。您想要使用的大多数第三方库必须下载，编译，然后安装自己。  
JVM是开发人员目标的标准机器。 C ++没有标准机器，它取决于开发人员了解他们的硬件并优化它周围的代码（即使他们开发的机器以及它们将部署到不同）。这就是为什么甚至Apple正在移动更多才能使用自己的位码，嘿，这与Java字节码相似！ Java和C ++开发真的是两个不同的心态。关于JVM的正面是可以构建硬件以适应它（就像Android一直在做的那样，我们甚至可能会看到Java处理器的复兴），其中必须编写C ++以适应硬件。然而，这意味着，JVM将继续发展，所以罐子只会随着时间的推移而变得更好。同时在2011年的硬件中编译的C ++二进制文件可能甚至不能在2021年在大多数系统上兼容（尽管，但是，可以重新编译系统的C ++源代码，而C ++编译器可能为新的生成更好的二进制文件硬件）。  
虽然回到了游戏的答案。如果您想现在从90年代播放一些游戏，您必须希望工作室足够友好地努力将其更新到最新的平台。如果是出于某种原因，他们已经写过JVM运行，即使是现在和未来的硬件也会消耗，并且JVM可以支持（不重新编译）。  
